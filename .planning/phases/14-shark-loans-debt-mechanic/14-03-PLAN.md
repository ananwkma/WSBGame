---
phase: 14-shark-loans-debt-mechanic
plan: "03"
type: execute
wave: 3
depends_on:
  - "14-01"
  - "14-02"
files_modified: []
autonomous: false
requirements:
  - MECH-02

must_haves:
  truths:
    - "TypeScript build is clean with zero errors"
    - "Borrowing from Loan Shark adds cash and increases sharkDebt simultaneously"
    - "Debt compounds each turn — sharkDebt after turn N+1 equals round(sharkDebt_N * 1.20)"
    - "Wife messages reflect BANKRUPT bracket when debt pushes net worth negative"
    - "DEBT_SPIRAL ending fires when debt exceeds net worth at game end"
    - "SHARK DEBT row appears in Portfolio tab and hides when debt is zero"
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Full debt mechanic implementation"
      contains: "newDebt"
    - path: "src/components/Apps/Phone/MessageThread.tsx"
      provides: "SharkLoanPanel UI"
      contains: "SharkLoanPanel"
  key_links:
    - from: "src/components/Apps/Phone/MessageThread.tsx"
      to: "src/store/useGameStore.ts"
      via: "borrowFromShark action call"
      pattern: "borrowFromShark"
---

<objective>
Verify the complete Shark Loans mechanic works end-to-end: TypeScript clean build, functional borrow/compound loop, correct wife bracket response to debt, and DEBT_SPIRAL ending detection.

Purpose: This phase introduces fragile multi-point state changes (debt affecting getNetWorth, settledNetWorth, wife brackets, and ending detection). Human verification catches integration failures that TypeScript cannot.
Output: Confirmed working shark loan mechanic with validated gameplay loop.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript clean build verification</name>
  <files></files>
  <action>
Run the TypeScript compiler in no-emit mode to confirm zero type errors across all files modified in plans 14-01 and 14-02:

```bash
npx tsc --noEmit
```

If errors exist, fix them before continuing. Common issues to check:
- `borrowFromShark` missing from the store body (not just the interface)
- `sharkDebt` destructured but `get()` call missing it
- `pickSharkMessage` import path incorrect
- `gameStatus` selector type mismatch in SharkLoanPanel
  </action>
  <verify>`npx tsc --noEmit` exits with code 0 and no output.</verify>
  <done>TypeScript build is clean. Zero compiler errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full shark loan gameplay loop</name>
  <files></files>
  <action>
Run `npm run dev` and open the game. Perform these 7 checks:

**Check 1 — Loan Shark thread exists:**
Open Phone → CHAT tab. Loan Shark should appear in the contact list with the intro message "Heard you been losing big..."

**Check 2 — Borrow panel works:**
Click Loan Shark thread. A borrow panel with 4 buttons ($10k, $25k, $50k, $100k) should appear below messages. Click "$10k". Verify: (a) confirmation message appears in thread ("Done. $10,000 wired..."), (b) cash increases by $10,000 in Portfolio tab.

**Check 3 — SHARK DEBT row appears:**
After borrowing, go to Robbinghood → Portfolio tab. A red "SHARK DEBT" row showing "-$10,000" should be visible.

**Check 4 — Debt compounds on next turn:**
Click "NEXT DAY". Go back to Loan Shark thread. A new threatening message should appear. Check Portfolio tab — SHARK DEBT should now show ~$12,000 (20% increase: $10k × 1.20).

**Check 5 — Wife reacts to debt:**
Borrow enough ($100k multiple times) so total debt exceeds $100,000. Advance a turn. Wife messages should shift toward BANKRUPT tone rather than COMFORTABLE bracket.

**Check 6 — DEBT_SPIRAL ending:**
Borrow enough that total debt > portfolio value. Advance to Day 10. The ending should be DEBT_SPIRAL, not a wealth-based ending.

**Check 7 — Borrow disabled after game ends:**
After any game ends, open Loan Shark thread. The 4 borrow buttons should be grayed out / disabled.
  </action>
  <verify>User types "approved" confirming all 7 checks pass.</verify>
  <done>All 7 checks pass. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
All 7 manual checks pass. TypeScript build clean.
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- Loan Shark contact visible in chat list from turn 1
- Borrow panel functional: cash increases, sharkDebt increases, confirmation message appears
- SHARK DEBT shown in Portfolio tab in red when debt > 0
- Debt compounds at ~20% per turn
- Wife messages reflect true (debt-adjusted) net worth bracket
- DEBT_SPIRAL ending fires correctly when debt > net worth at game end
- Borrow buttons disabled after game ends
</success_criteria>

<output>
After completion, create `.planning/phases/14-shark-loans-debt-mechanic/14-03-SUMMARY.md`
</output>
