---
phase: 09-accurate-options-engine
plan: 09-01-PLAN
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/marketUtils.ts, src/store/types.ts, src/store/useGameStore.ts]
autonomous: true
requirements: [TRADE-09, TRADE-11]

must_haves:
  truths:
    - "Options premiums are calculated via Black-Scholes formula."
    - "Greeks (Delta, Gamma, Theta, Vega) are derived from the BS model."
    - "1DTE options maintain non-zero value at high IV levels."
  artifacts:
    - path: "src/utils/marketUtils.ts"
      provides: "stdNormalCDF, stdNormalPDF, calculateBS functions"
    - path: "src/store/types.ts"
      provides: "Updated OptionContract and StockData types with vega and iv"
    - path: "src/store/useGameStore.ts"
      provides: "Refactored Greeks and Pricing utilities using the BS engine"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "src/utils/marketUtils.ts"
      via: "import { calculateBS }"
---

<objective>
Implement the Black-Scholes options pricing engine and refactor the financial utilities to use it.

Purpose: Provide a mathematically accurate foundation for options trading, ensuring realistic premium behavior and consistent Greeks.
Output: Stateless BS utility and updated game store pricing logic.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09/09-RESEARCH.md
@src/utils/marketUtils.ts
@src/store/types.ts
@src/store/useGameStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Black-Scholes Math in marketUtils.ts</name>
  <files>src/utils/marketUtils.ts</files>
  <action>
    - Implement `stdNormalCDF(x: number)` using the Abramowitz & Stegun approximation (as provided in 09-RESEARCH.md).
    - Implement `stdNormalPDF(x: number)`.
    - Implement `calculateBS(type, s, k, t, v, r)` function that returns { price, delta, gamma, theta, vega }.
    - Use T = daysToExpiry / 252 for annualization.
    - Ensure T <= 0.0001 returns intrinsic value and 0 greeks (except delta).
    - Theta should be scaled by 1/252 to represent daily decay.
    - Vega should represent change for 1% IV shift (standard).
  </action>
  <verify>Run manual check or temporary script: calculateBS('CALL', 100, 100, 1/252, 1.0, 0.05) should yield a price around ~2.60.</verify>
  <done>BS math functions are exported and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Update Options and Stock Types</name>
  <files>src/store/types.ts</files>
  <action>
    - Update `OptionContract` interface to include `vega: number`.
    - Update `StockData` interface to include `iv: number`.
    - Update `buyOption` in `GameActions` to accept `vega` in the `greeks` parameter.
  </action>
  <verify>Types compile successfully.</verify>
  <done>Types include vega and iv.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Store Financial Utilities</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Import `calculateBS` from `marketUtils.ts`.
    - Refactor `calculateDelta`, `calculateGamma`, `calculateTheta`, and `calculateOptionPrice` to accept `iv` and `t` (default `t = 1/252`).
    - Implement `calculateVega(price, strike, iv, t)`.
    - Update `generateOptionsChain` to use the stock's `iv` and the new `calculateBS`-backed functions.
    - Update `buyOption` implementation to store the calculated `vega`.
    - Update callers of `calculateOptionPrice` (e.g. in `buyOption`, `sellOption`, `getNetWorth`) to pass the stock's current `iv`.
  </action>
  <verify>Check that options chain displays reasonable prices (OTM calls should be cheaper than ATM).</verify>
  <done>Store pricing logic uses Black-Scholes.</done>
</task>

</tasks>

<verification>
- Verification Script: `console.log(calculateBS('CALL', 10000, 10500, 1/252, 1.5))` in a temporary dev environment.
- Confirm that Theta increases (more negative) as expiry approaches in the engine.
- Confirm Vega is highest for ATM options.
</verification>

<success_criteria>
- calculateBS returns mathematically consistent values.
- Option premiums in the game reflect volatility and time decay realistically.
- Greeks are no longer estimated via tanh/moneyness approximations.
</success_criteria>

<output>
After completion, create `.planning/phases/09/09-01-SUMMARY.md`
</output>
