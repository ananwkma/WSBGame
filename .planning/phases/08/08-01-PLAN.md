---
phase: 08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Trade/PriceChart.tsx, src/components/Trade/Robbinghood.tsx]
autonomous: true
requirements: [TRADE-04]
---

<objective>
Fix the misleading "green line" on charts when overall in a loss, and ensure the live net worth data point doesn't create visual artifacts or redundant points in the history trace.

Purpose: Improve visual feedback accuracy for portfolio performance.
Output: Correctly colored charts and clean net worth history visualization.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08/08-RESEARCH.md
@src/components/Trade/PriceChart.tsx
@src/components/Trade/Robbinghood.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PriceChart Color Logic</name>
  <files>src/components/Trade/PriceChart.tsx</files>
  <action>
    Modify the `chartColor` useMemo in `PriceChart.tsx` to compare the `current` value against the `initial` value of the history array instead of the `previous` value.
    - Extract `initial` value: `const initial = getValue(history[0])`.
    - Update return condition: `return current >= initial ? '#94ba8b' : '#ba8b8b'`.
    - This ensures that if the user is currently at $50k but started the view at $100k, the line is red, even if the last step was a slight gain.
  </action>
  <verify>Check PriceChart.tsx and ensure it now compares against the first element of the history array.</verify>
  <done>Chart color reflects performance relative to the start of the time series.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent Duplicate Points in Robbinghood Net Worth History</name>
  <files>src/components/Trade/Robbinghood.tsx</files>
  <action>
    In `Robbinghood.tsx`, find the `PriceChart` usage within the 'Portfolio' tab.
    Wrap the history construction in a `useMemo` that ensures we don't add a "live" point if it's redundant or has a duplicate turn.
    - Logic: 
      ```typescript
      const chartHistory = useMemo(() => {
        const history = [...netWorthHistory];
        const lastPoint = history[history.length - 1];
        // Only add a live point if it's actually ahead in time
        if (lastPoint && lastPoint.turn < day + 0.5) {
          history.push({ turn: day + 0.5, value: netWorth });
        }
        return history;
      }, [netWorthHistory, day, netWorth]);
      ```
    - Pass `chartHistory` to the `PriceChart` component instead of the inline array literal.
  </action>
  <verify>Check Robbinghood.tsx and confirm the PriceChart history is derived using a memoized filter to prevent duplicate turn indices.</verify>
  <done>Robbinghood Portfolio chart uses a clean history array with a single live point for the current turn.</done>
</task>

</tasks>

<verification>
Check both files for implementation of the requested fixes.
Run the game and verify that the Portfolio chart correctly turns red if Net Worth is below the starting $100,000.
</verification>

<success_criteria>
- PriceChart color reflects overall gain/loss relative to the start of the history array.
- Robbinghood Portfolio view correctly renders the net worth chart without visual glitches from duplicate points.
</success_criteria>

<output>
After completion, create `.planning/phases/08/08-01-SUMMARY.md`
</output>
