---
phase: 15-live-market-events-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Apps/Phone/PhoneApp.tsx
  - src/components/Shell/DualViewShell.tsx
  - src/components/Laptop/LaptopBrowser.tsx
  - src/components/Laptop/ReaditTab.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Phone header shows 'uPhone' instead of 'PHONE v1.0'"
    - "Phone tabs show 'uMessage' and 'readit' instead of 'CHAT' and 'FORUM'"
    - "Phone has no HYPE LEVEL bar — the fomo-meter-container block is fully removed"
    - "DualViewShell no longer jitters — the hype-driven animate prop is removed"
    - "Laptop browser has three tabs: Robbinghood, GuruTube, readit"
    - "Laptop readit tab shows the u/DegenTrader onboarding post with troll tutorial replies"
    - "App still compiles and renders without errors after all renames"
  artifacts:
    - path: "src/components/Apps/Phone/PhoneApp.tsx"
      provides: "Renamed labels: uPhone, uMessage, readit tabs; removed HYPE LEVEL bar"
      contains: "uPhone"
    - path: "src/components/Shell/DualViewShell.tsx"
      provides: "Static (non-jittering) shell — hype animate removed"
      contains: "animate={{ x: 0"
    - path: "src/components/Laptop/LaptopBrowser.tsx"
      provides: "Three-tab layout with READIT tab added"
      contains: "READIT"
    - path: "src/components/Laptop/ReaditTab.tsx"
      provides: "Onboarding post: u/DegenTrader $100k→$1M challenge with 6 troll tutorial replies"
      contains: "DegenTrader"
  key_links:
    - from: "src/components/Laptop/LaptopBrowser.tsx"
      to: "src/components/Laptop/ReaditTab.tsx"
      via: "import and render when activeTab === 'READIT'"
      pattern: "ReaditTab"
---

<objective>
Apply all branding/label renames and UI cleanup: remove the HYPE LEVEL bar from the phone, remove the hype jitter from DualViewShell, rename phone tabs to uMessage/readit, rename phone header to uPhone, add a third 'readit' tab to the laptop browser, and create the ReaditTab onboarding post.

Purpose: This plan is independent of the real-time engine (Plan 01) — it touches only UI components with no store dependencies beyond label strings. Running it in Wave 1 (parallel with Plan 01) keeps total execution time down.

Output: Rebranded phone UI, jitter-free shell, three-tab laptop, and the onboarding readit post.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-live-market-events-polish/15-CONTEXT.md
@.planning/phases/15-live-market-events-polish/15-RESEARCH.md
@src/components/Apps/Phone/PhoneApp.tsx
@src/components/Shell/DualViewShell.tsx
@src/components/Laptop/LaptopBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phone rebranding and hype bar removal</name>
  <files>src/components/Apps/Phone/PhoneApp.tsx, src/components/Shell/DualViewShell.tsx</files>
  <action>
Read both files fully before editing.

**PhoneApp.tsx changes (per RESEARCH.md branding rename map):**

1. Change `type PhoneTab = 'CHAT' | 'FORUM'` to `type PhoneTab = 'UMESSAGE' | 'READIT'`
2. Change `useState<PhoneTab>('CHAT')` to `useState<PhoneTab>('UMESSAGE')`
3. Remove the `const hype = useGameStore(...)` selector line entirely
4. Remove the entire `.fomo-meter-container` JSX block (the HYPE LEVEL bar) — this is the block that shows the hype percentage bar, typically 10–15 lines of JSX
5. Change the phone header label text from `PHONE v1.0` to `uPhone`
6. Change the tab button label `CHAT` to `uMessage`
7. Change the tab button label `FORUM` to `readit`
8. Update any conditional rendering that checks `activeTab === 'CHAT'` to check `activeTab === 'UMESSAGE'`
9. Update any conditional rendering that checks `activeTab === 'FORUM'` to check `activeTab === 'READIT'`

**DualViewShell.tsx changes (per RESEARCH.md hype removal map):**

1. Delete `const hype = useGameStore(...)` line
2. Delete `const isHighHype = hype > 80;` line
3. Find the `motion` element (or framer-motion element) that uses `animate={isHighHype ? { x: [...], y: [...] } : { x: 0, y: 0 }}` and replace with `animate={{ x: 0, y: 0 }}` (static — no jitter ever)
4. Remove any unused import of `useGameStore` if it is no longer referenced after this change

After edits: `npx tsc --noEmit` should show zero new errors from these two files.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20 && grep -n "hype\|HYPE\|PHONE v1.0\|CHAT\|FORUM" src/components/Apps/Phone/PhoneApp.tsx src/components/Shell/DualViewShell.tsx</verify>
  <done>No TypeScript errors from these files. grep returns no matches for hype/HYPE/PHONE v1.0/CHAT/FORUM in PhoneApp.tsx and DualViewShell.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Add readit tab to laptop browser and create ReaditTab onboarding post</name>
  <files>src/components/Laptop/LaptopBrowser.tsx, src/components/Laptop/ReaditTab.tsx</files>
  <action>
Read src/components/Laptop/LaptopBrowser.tsx fully before editing.

**LaptopBrowser.tsx changes:**

1. Find `type LaptopTab = 'ROBBINGHOOD' | 'GURUTUBE'` and change to `type LaptopTab = 'ROBBINGHOOD' | 'GURUTUBE' | 'READIT'`
2. Add a third tab button alongside the existing two:
   ```tsx
   <button
     className={`laptop-tab-btn${activeTab === 'READIT' ? ' active' : ''}`}
     onClick={() => setActiveTab('READIT')}
   >
     readit
   </button>
   ```
3. Add conditional rendering for the readit tab content:
   ```tsx
   {activeTab === 'READIT' && <ReaditTab />}
   ```
4. Add import: `import { ReaditTab } from './ReaditTab';`

**Create src/components/Laptop/ReaditTab.tsx (new file):**

This is the r/wallstreetbets onboarding post. It should look like a forum post thread using existing pixel.css classes (use whatever forum/feed CSS classes already exist in the project for consistency — inspect LaptopBrowser or forum components for class names to reuse).

Content requirements (per CONTEXT.md decisions):
- Post author: `u/DegenTrader`
- Post title: something like "10-day $100k to $1M YOLO challenge — day 1 let's get it 🚀🚀🚀" (can use emoji in post text since it's in the feed, not ASCII art)
- Post body: 2-3 sentences announcing the challenge. Written in authentic WSB style — degenerate, over-confident, uses "retard" self-deprecatingly, mentions options.
- Flair: "YOLO"
- Upvotes: something like "4.2k" displayed

Replies (6 troll-style replies that each naturally embed one game mechanic as community knowledge):
1. u/StockDaddy69: Something dismissive that mentions the NEXT DAY button advances time, mocking OP for not knowing
2. u/DiamondHandsDave: Comment about buying stocks (long shares) vs options — calls out OP for definitely going to buy calls and lose everything
3. u/BoboTheBear: Comment about the Loan Shark — warns about borrowing if the player gets margin called, in a threatening/joking way
4. u/PaperHandsPete: Comment about the phone (uMessage/readit) vs laptop (Robbinghood/GuruTube) layout — something like "don't forget to check your phone for the wife texts lmaooo"
5. u/RetardStrength: Comment about options expiry — mentions that options expire at end of each day and warns OP not to hold them into the close
6. u/MoonMission2024: Comment about the GuruTube gurus — warns that gurus are often wrong and pump-and-dump, but in a way that teaches the player to use GuruTube

Each reply should be 1-2 sentences, written in authentic WSB tone (crude, meme-heavy, self-aware), NOT instructional text. The tutorial should feel emergent, not scripted.

Style notes:
- Use the existing pixel aesthetic classes — match the look of forum posts elsewhere in the app
- Upvote counts and usernames should look like real WSB comment formatting
- No separate CSS file needed — use existing pixel.css utility classes
- The post is static (no live data binding) — it never changes during a game session
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20 && ls src/components/Laptop/ReaditTab.tsx</verify>
  <done>Zero TypeScript errors. ReaditTab.tsx exists. LaptopBrowser.tsx has 'READIT' in the LaptopTab type definition.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `grep "uPhone\|uMessage\|readit" src/components/Apps/Phone/PhoneApp.tsx` returns matches
3. `grep "hype\|HYPE\|fomo-meter" src/components/Apps/Phone/PhoneApp.tsx src/components/Shell/DualViewShell.tsx` returns no results
4. `grep "READIT" src/components/Laptop/LaptopBrowser.tsx` returns the type and conditional
5. `cat src/components/Laptop/ReaditTab.tsx` shows u/DegenTrader post and 6 replies
</verification>

<success_criteria>
- Phone header shows "uPhone", tabs show "uMessage" and "readit", HYPE LEVEL bar is gone
- DualViewShell is static with no hype-driven animation
- Laptop has three tabs; readit tab renders ReaditTab.tsx
- ReaditTab.tsx has the u/DegenTrader post plus 6 WSB-style troll tutorial replies
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-live-market-events-polish/15-02-SUMMARY.md`
</output>
