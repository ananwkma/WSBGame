---
phase: 16-qol-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/Trade/Robbinghood.tsx
  - src/components/Laptop/ReaditTab.tsx
autonomous: false
requirements:
  - UX-02
must_haves:
  truths:
    - "NEXT DAY button is visually grayed out and non-clickable while the market is open or has not yet closed (marketTime < 960)"
    - "Buy and sell swipe actions are disabled after market close — swiping does nothing and the component shows as disabled"
    - "Readit laptop forum text is legible white/cream on the dark forum-post background"
  artifacts:
    - path: "src/App.tsx"
      provides: "NEXT DAY button gating via disabled prop and opacity style"
      contains: "disabled={marketTime < 960}"
    - path: "src/components/Trade/Robbinghood.tsx"
      provides: "All four SwipeConfirm instances gated by marketIsOpen"
      contains: "!marketIsOpen"
    - path: "src/components/Laptop/ReaditTab.tsx"
      provides: "Explicit color on outermost wrapper div"
      contains: "color: '#e0dbcb'"
  key_links:
    - from: "src/App.tsx"
      to: "useGameStore.marketTime"
      via: "useGameStore selector already destructured at line 38"
      pattern: "marketTime < 960"
    - from: "src/components/Trade/Robbinghood.tsx"
      to: "useGameStore.marketIsOpen"
      via: "marketIsOpen already destructured at line 54"
      pattern: "!marketIsOpen"
---

<objective>
Gate NEXT DAY behind market close, disable all trading SwipeConfirm actions after market close, and fix invisible Readit forum text.

Purpose: Players currently exploit NEXT DAY mid-session and can trade after hours. Readit text is unreadable (black on dark background).
Output: App.tsx with disabled NEXT DAY, Robbinghood.tsx with 4 gated SwipeConfirms, ReaditTab.tsx with legible text color.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-qol-polish/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate NEXT DAY button and fix Readit text color</name>
  <files>src/App.tsx, src/components/Laptop/ReaditTab.tsx</files>
  <action>
In src/App.tsx:
- The NEXT DAY button is at line 49. marketTime is already destructured at line 38 and marketIsOpen at line 39.
- Change the button element to add disabled prop and visual feedback:
  disabled={marketTime < 960}
  style={{ position: 'static', opacity: marketTime < 960 ? 0.4 : 1, cursor: marketTime < 960 ? 'not-allowed' : 'pointer' }}
- Use condition marketTime < 960 (NOT !marketIsOpen). marketIsOpen also becomes false during pre-market (before 570) which would wrongly block the button at game start. The correct gate is "has the market closed today" = marketTime >= 960.

In src/components/Laptop/ReaditTab.tsx:
- The outermost div is at line 51 with className="phone-app-content" and inline style with padding/overflowY/height/boxSizing.
- Add color: '#e0dbcb' to that inline style object.
- Root cause: .forum-post sets background-color: #2b2b26 (dark) but no color property, so it inherits black from the browser-content parent. The explicit color on the wrapper overrides this cascade.
  </action>
  <verify>Run npm run build — zero TypeScript errors. During market hours NEXT DAY button is dim (opacity 0.4) and non-clickable. After marketTime >= 960 it becomes bright and clickable. Readit laptop tab shows white/cream text on dark forum-post backgrounds.</verify>
  <done>NEXT DAY disabled with opacity 0.4 and cursor not-allowed when marketTime < 960. ReaditTab outermost div has color '#e0dbcb'.</done>
</task>

<task type="auto">
  <name>Task 2: Gate all four trading SwipeConfirm instances after market close</name>
  <files>src/components/Trade/Robbinghood.tsx</files>
  <action>
In src/components/Trade/Robbinghood.tsx:
- marketIsOpen is already destructured from the store at line 54.
- There are 4 SwipeConfirm instances around lines 535-549. Add !marketIsOpen to each disabled condition:

1. Stock BUY SwipeConfirm (line 535): disabled={tradeAmount === 0 || !marketIsOpen}
2. Stock SELL SwipeConfirm (line 536): disabled={tradeAmount === 0 || !marketIsOpen}
3. Option BUY SwipeConfirm (lines 540-544): disabled={!selectedOptionData || tradeAmount === 0 || !marketIsOpen}
4. Option SELL SwipeConfirm (lines 546-549): disabled={!selectedOptionData || tradeAmount === 0 || heldOptionAmount < tradeAmount || !marketIsOpen}

Do NOT disable the ALL IN / SELL ALL shortcut buttons — those only set local tradeAmount state, not execute trades. The SwipeConfirm gate is the execution blocker.

The OptionsChain selector (browsing strikes) should remain active after close — browsing is fine, only execution is blocked.
  </action>
  <verify>Run npm run build — zero TypeScript errors. After market close (marketTime >= 960): all 4 SwipeConfirm components appear disabled and swiping does not call handleBuy or handleSell.</verify>
  <done>All 4 SwipeConfirm instances have || !marketIsOpen in their disabled prop. Buy and sell execution is blocked post-market-close.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify market gating and Readit text visibility</name>
  <files></files>
  <action>
Run npm run dev and open the game. Perform these 6 checks:

Check 1 — NEXT DAY disabled during market hours:
While the MARKET OPEN indicator is shown (green dot), the NEXT DAY button should appear grayed out (opacity ~0.4) and clicking it should do nothing.

Check 2 — NEXT DAY enabled after market close:
Use the debug +1h button to advance time until MARKET CLOSED appears. The NEXT DAY button should now appear bright and be clickable.

Check 3 — Trade blocked during market hours:
While market is open, go to Robbinghood and navigate to the Trade tab for any stock. Try to swipe the BUY or SELL SwipeConfirm — it should not respond (SwipeConfirm shows as disabled).

Check 4 — Trade blocked after market close:
After market closes, confirm BUY and SELL swipe actions are still blocked.

Check 5 — Options trade blocked:
Navigate to an options chain and confirm the option BUY and option SELL SwipeConfirm components are also disabled after market close.

Check 6 — Readit text visible:
Open laptop, click the readit tab. All forum post body text (the tutorial tips below each username) should be white or cream-colored, not black on dark background.
  </action>
  <verify>All 6 checks pass without issues.</verify>
  <done>All market-hour gates and Readit text fix confirmed working in-browser.</done>
</task>

</tasks>

<verification>
- npm run build exits 0 with no TypeScript errors
- App.tsx NEXT DAY button has disabled={marketTime < 960}
- All 4 SwipeConfirm disabled props include !marketIsOpen
- ReaditTab outermost div inline style has color: '#e0dbcb'
</verification>

<success_criteria>
1. NEXT DAY button is visually grayed out and non-functional while market is open (marketTime 570 to 959).
2. NEXT DAY button is active after market close (marketTime >= 960).
3. All buy/sell stock and option actions are blocked after market close.
4. Readit laptop forum text is legible on dark background.
5. TypeScript build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/16-qol-polish/16-01-SUMMARY.md`
</output>
