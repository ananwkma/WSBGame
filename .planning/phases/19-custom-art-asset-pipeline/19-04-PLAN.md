---
phase: 19-custom-art-asset-pipeline
plan: "04"
type: execute
wave: 3
depends_on:
  - "19-02"
  - "19-03"
files_modified: []
autonomous: false
requirements:
  - ART-01

must_haves:
  truths:
    - "All 5 asset swap points are visible in-browser with gray placeholder images"
    - "Game Boy 4-color filter is applied to placeholders (visible in game view)"
    - "Ending screens show gray rectangle instead of ASCII pixel art"
    - "GuruTube shows gray rectangle face instead of emoji"
    - "Phone lock screen shows gray bg image behind notification content"
    - "Contact list shows gray square photos instead of emoji"
    - "ReaditTab MY PROFILE sidebar shows gray avatar above u/DegenTrader"
    - "TypeScript build is clean and production build succeeds"
  artifacts:
    - path: "src/components/Feedback/EndingScreen.tsx"
      provides: "Verified: img renders in browser"
    - path: "src/components/Laptop/GuruTube.tsx"
      provides: "Verified: img renders in browser"
    - path: "src/components/Apps/Phone/PhoneApp.tsx"
      provides: "Verified: lock screen bg image visible"
    - path: "src/components/Apps/Phone/MessageList.tsx"
      provides: "Verified: contact img renders"
    - path: "src/components/Laptop/ReaditTab.tsx"
      provides: "Verified: avatar img renders"
  key_links:
    - from: "art-assets branch"
      to: "master"
      via: "future rebase when master advances"
      pattern: "rebase.*master"
---

<objective>
Final TypeScript/production build check followed by human verification that all 5 asset swap types render correctly in the browser on the art-assets branch.

Purpose: Confirms the pipeline works end-to-end — placeholder images load, the 4-color filter applies, and no broken images appear in any of the 5 asset locations.
Output: Confirmed working art asset pipeline on the art-assets branch.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-custom-art-asset-pipeline/19-02-SUMMARY.md
@.planning/phases/19-custom-art-asset-pipeline/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files>N/A</files>
  <action>
    Run the following verification commands and confirm all pass:

    1. `git branch` — confirm art-assets is current branch (NOT master)
    2. `npx tsc --noEmit` — TypeScript clean build, must exit 0
    3. `npm run build` — Vite production build, must exit 0
    4. `npm run dev` — start dev server for visual verification

    If tsc or build fails, fix the issue before proceeding to the checkpoint. Common issues:
    - Import of removed PixelArt component still exists somewhere → remove it
    - Missing type annotation on contactNameToFilename → add `: string` return type
    - Unused variable warning treated as error → remove the unused variable

    After confirming the build passes, leave `npm run dev` running for the visual check.
  </action>
  <verify>`npx tsc --noEmit` exits 0. `npm run build` exits 0 with no errors. Dev server starts on localhost:5173.</verify>
  <done>TypeScript and production build both pass cleanly. Dev server is running for visual inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 5 asset swap points</name>
  <files></files>
  <action>
    With dev server running at http://localhost:5173, perform these 7 checks:

    **Check 1 — Ending screen placeholder:**
    Play a game to completion (use clock speed 5x to speed up). When the ending screen appears, confirm a gray rectangle placeholder appears where the ASCII pixel art used to be. The placeholder should be labeled (e.g., "MENDYS"). It will be filtered through the Game Boy palette (appears as a grayscale tinted rectangle).

    **Check 2 — GuruTube face placeholder:**
    Open the laptop, click GuruTube. Confirm the guru face area shows a gray square placeholder (labeled "GURU_HAPPY_IDLE" or similar) instead of the emoji face. The placeholder should pulse/scale slightly — confirming the animation outer div is intact.

    **Check 3 — Phone lock screen background:**
    Click the laptop panel so the phone shows its lock screen. Confirm a gray rectangle appears as the lock screen background behind the clock/notifications text. Click the lock screen — it must still unlock (confirms pointerEvents: none is working).

    **Check 4 — Contact list avatars:**
    Unlock the phone, open Messages (CHAT tab). Confirm each contact thread shows a gray 40x40 square instead of an emoji. Open any thread — confirm the thread header also shows a gray square avatar instead of an emoji.

    **Check 5 — ReaditTab profile avatar:**
    Open the laptop, click the Readit tab. In the right sidebar under MY PROFILE, confirm a 48x48 gray square appears above the u/DegenTrader username text.

    **Check 6 — No broken image icons:**
    Confirm no broken-image browser icons appear anywhere. Open browser DevTools Network tab, filter by "Img" — all /assets/... requests must return 200 (not 404).

    **Check 7 — ASSETS.md exists:**
    Confirm public/assets/ASSETS.md exists with all 22 asset file paths listed. Run `cat public/assets/ASSETS.md` or check the file in the editor.
  </action>
  <verify>All 7 checks pass: placeholders visible in all 5 locations, no broken images, ASSETS.md present.</verify>
  <done>All 5 visual asset swap points confirmed working in-browser. No broken images. ASSETS.md present. Type "approved" to complete the phase.</done>
</task>

</tasks>

<verification>
All 7 in-browser checks pass. TypeScript and production build clean. No broken images. All /assets/... requests return 200.
</verification>

<success_criteria>
The art-assets branch is a fully working swap pipeline. An artist can clone the repo, checkout art-assets, replace any file in public/assets/ with real artwork at the documented dimensions, and the game will immediately use their art. ASSETS.md gives them all the information they need without reading code.
</success_criteria>

<output>
After completion, create `.planning/phases/19-custom-art-asset-pipeline/19-04-SUMMARY.md`
</output>
