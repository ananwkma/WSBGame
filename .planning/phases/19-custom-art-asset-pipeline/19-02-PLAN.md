---
phase: 19-custom-art-asset-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/components/Feedback/EndingScreen.tsx
  - src/components/Laptop/GuruTube.tsx
autonomous: true
requirements:
  - ART-01

must_haves:
  truths:
    - "EndingScreen renders <img> from /assets/endings/<TYPE>.png instead of PixelArt SVG component"
    - "GuruTube renders <img> from /assets/guru/<mood-state>.png instead of emoji inside <svg><text>"
    - "TypeScript builds cleanly (tsc --noEmit exits 0) after both component changes"
  artifacts:
    - path: "src/components/Feedback/EndingScreen.tsx"
      provides: "Ending screen with <img> swap replacing PixelArt"
      contains: "/assets/endings/"
    - path: "src/components/Laptop/GuruTube.tsx"
      provides: "GuruTube with <img> swap replacing emoji SVG"
      contains: "/assets/guru/"
  key_links:
    - from: "src/components/Feedback/EndingScreen.tsx"
      to: "public/assets/endings/<TYPE>.png"
      via: "template literal src={`/assets/endings/${result}.png`}"
      pattern: "/assets/endings/\\$\\{result\\}"
    - from: "src/components/Laptop/GuruTube.tsx"
      to: "public/assets/guru/<mood>.png"
      via: "getGuruFaceSrc() returning /assets/guru/... path"
      pattern: "/assets/guru/"
---

<objective>
On the art-assets branch, replace hardcoded SVG/emoji visuals in EndingScreen.tsx and GuruTube.tsx with <img> tags pointing to the placeholder PNGs created in plan 01.

Purpose: Wires the ending art and guru face asset slots so dropping new PNGs into public/assets/ instantly updates the game visuals.
Output: Modified EndingScreen.tsx and GuruTube.tsx on the art-assets branch.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-custom-art-asset-pipeline/19-CONTEXT.md
@.planning/phases/19-custom-art-asset-pipeline/19-RESEARCH.md
@.planning/phases/19-custom-art-asset-pipeline/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap EndingScreen PixelArt SVG for <img></name>
  <files>src/components/Feedback/EndingScreen.tsx</files>
  <action>
    Read the current `EndingScreen.tsx` in full. The component uses a `PixelArt` component rendering a 16x12 pixel grid at PIXEL_SIZE=7 (112x84 px output). The `ENDING_CONTENT` Record maps each EndingType to `{ pixelArt: string[] }` among other fields.

    **Changes on the art-assets branch:**

    1. Find the `<PixelArt grid={content.pixelArt} />` render call and replace it with:
    ```tsx
    <img
      src={`/assets/endings/${result}.png`}
      alt={result}
      width={112}
      height={84}
      style={{ display: 'block', margin: '16px auto', imageRendering: 'pixelated' }}
    />
    ```
    Where `result` is the EndingType string prop passed to the component (e.g. `'MENDYS'`, `'DEBT_SPIRAL'`). Confirm the variable name by reading the component.

    2. Remove the `pixelArt` field from each entry in `ENDING_CONTENT` — the arrays of strings are no longer needed. Keep all other fields (title, description, color, etc.) intact.

    3. If `PixelArt` is defined in this file as a local component, remove its definition. If imported from elsewhere, remove the import.

    4. Do NOT use `import` syntax for the PNG path (e.g., `import endingImg from '../...'`). Use the string literal `/assets/endings/${result}.png` directly — Vite serves `public/` at the URL root with no bundling.

    5. Confirm `result` (the EndingType variable used in the JSX) is what flows into the template literal. The filename convention is all-caps enum value exactly (MENDYS, BREAK_EVEN, etc.).

    After editing, run `npx tsc --noEmit` to verify TypeScript is clean.
  </action>
  <verify>Run `npx tsc --noEmit` — must exit 0. Grep for `PixelArt` in EndingScreen.tsx — must return no results. Grep for `/assets/endings/` in EndingScreen.tsx — must find the img src template literal.</verify>
  <done>EndingScreen.tsx renders an <img> from /assets/endings/${result}.png. The PixelArt component and all pixelArt string arrays are removed. TypeScript builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Swap GuruTube emoji SVG for <img></name>
  <files>src/components/Laptop/GuruTube.tsx</files>
  <action>
    Read the current `GuruTube.tsx` in full. The component animates a guru face using `marketSentiment` (HAPPY/SAD) and `frame` (0/1) state, rendering an emoji inside an `<svg><text>` element. There is an outer animated div that applies `transform: scale(1.05)` on frame 1.

    **Changes on the art-assets branch:**

    1. Add a `getGuruFaceSrc` helper function (inside the component or as a module-level pure function):
    ```tsx
    const getGuruFaceSrc = () => {
      if (marketSentiment === 'HAPPY') {
        return frame === 0
          ? '/assets/guru/happy-idle.png'
          : '/assets/guru/happy-excited.png';
      }
      return frame === 0
        ? '/assets/guru/sad-worried.png'
        : '/assets/guru/sad-crying.png';
    };
    ```

    2. Find the `<svg viewBox="0 0 100 100" ...><text ...>{getGuruFace()}</text></svg>` block (or equivalent emoji render block) and replace it entirely with:
    ```tsx
    <img
      src={getGuruFaceSrc()}
      alt="guru"
      style={{
        width: '85%',
        height: '85%',
        imageRendering: 'pixelated',
        objectFit: 'contain',
      }}
    />
    ```

    3. Keep the outer animated div with its scale pulse transform — the `<img>` becomes a child of this div, same as the SVG was. The animation behavior is preserved.

    4. Remove the old `getGuruFace()` function (the one returning emoji strings) and any emoji-related imports if they exist. Do NOT touch any other logic in GuruTube.tsx.

    5. Do NOT add `pointerEvents: 'none'` to the img here (that is needed only for the lock screen). The GuruTube img can receive pointer events normally.

    After editing, run `npx tsc --noEmit` to verify TypeScript is clean.
  </action>
  <verify>Run `npx tsc --noEmit` — must exit 0. Grep for `getGuruFace` in GuruTube.tsx — the old emoji function must be gone. Grep for `/assets/guru/` in GuruTube.tsx — must find the image src strings. The outer animated div structure must still be present (grep for 'scale' in GuruTube.tsx).</verify>
  <done>GuruTube.tsx renders an <img> using getGuruFaceSrc() for all 4 mood states. The emoji SVG block is removed. Scale pulse animation outer div is intact. TypeScript builds cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 after both changes
- `grep -r "PixelArt" src/components/Feedback/EndingScreen.tsx` returns no matches
- `grep "/assets/endings/" src/components/Feedback/EndingScreen.tsx` returns the img src template literal
- `grep "getGuruFace[^S]" src/components/Laptop/GuruTube.tsx` returns no matches (old function gone)
- `grep "/assets/guru/" src/components/Laptop/GuruTube.tsx` returns 4 path strings
- `npm run build` exits 0 (Vite production build)
</verification>

<success_criteria>
EndingScreen.tsx and GuruTube.tsx on the art-assets branch render <img> tags pointing to /assets/... paths. TypeScript clean. No broken imports. Original logic (game state, animation, etc.) is unchanged — only the visual render element is swapped.
</success_criteria>

<output>
After completion, create `.planning/phases/19-custom-art-asset-pipeline/19-02-SUMMARY.md`
</output>
