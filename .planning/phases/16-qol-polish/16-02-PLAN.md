---
phase: 16-qol-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/useGameStore.ts
  - src/store/types.ts
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Text messages from wife and extra contacts arrive at random times during market hours, not all at day start"
    - "A net-worth swing of 10% or more from day open triggers an immediate wife message mid-session, once per day max"
    - "Advancing a day discards undelivered previous-day pending messages so the queue never grows unboundedly"
  artifacts:
    - path: "src/store/types.ts"
      provides: "netWorthTriggerFiredToday field in GameState"
      contains: "netWorthTriggerFiredToday"
    - path: "src/store/useGameStore.ts"
      provides: "advanceDay schedules wife and extra-contact messages as ScheduledMessage with random deliverAt 570-959; tickMarket swing detection"
      contains: "netWorthTriggerFiredToday"
  key_links:
    - from: "src/store/useGameStore.ts advanceDay"
      to: "pendingMessages"
      via: "Wife and extra-contact messages wrapped in ScheduledMessage with random deliverAt instead of direct thread push"
      pattern: "pendingMessages.*ScheduledMessage"
    - from: "src/store/useGameStore.ts tickMarket"
      to: "pendingMessages"
      via: "Swing detection adds immediate ScheduledMessage when swingPct >= 0.10 and !netWorthTriggerFiredToday"
      pattern: "swingPct"
---

<objective>
Refactor advanceDay to schedule narrative messages (wife, extra contacts) as pending mid-session deliveries instead of front-loading them at day start. Add a net-worth swing trigger in tickMarket that sends one immediate wife message when net worth moves 10% or more from day open.

Purpose: Messages currently arrive all at once when player clicks NEXT DAY, breaking immersion. The pendingMessages + tickMarket delivery infrastructure from Phase 15 already works but advanceDay never populated it.
Output: Modified useGameStore.ts and types.ts with intraday message scheduling.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-qol-polish/16-RESEARCH.md
@src/store/useGameStore.ts
@src/store/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add netWorthTriggerFiredToday to GameState, initialize, and reset in advanceDay</name>
  <files>src/store/types.ts, src/store/useGameStore.ts</files>
  <action>
In src/store/types.ts:
- In the GameState interface, add a new field near bigGainTicker / bigLossTicker (other transient fields):
    netWorthTriggerFiredToday: boolean;

In src/store/useGameStore.ts:
- In getInitialState() (around line 380-440), add to the returned object:
    netWorthTriggerFiredToday: false,

- In the partialize exclusion destructuring (around line 1449-1454), add netWorthTriggerFiredToday to the excluded list alongside bigGainTicker and bigLossTicker so it is NOT persisted to localStorage:
    const { lastFlash, popups, intradayBars, netWorthBars,
            pendingMessages, activeEvents, scheduledEvents, marketTime, marketIsOpen,
            bigGainTicker, bigLossTicker, netWorthTriggerFiredToday, ...rest } = state;

- In the advanceDay set() call (around line 1398-1424 where marketTime: 480 and intradayBars: {} are reset), add two fields:
    netWorthTriggerFiredToday: false,
    pendingMessages: [],
  The pendingMessages reset discards any undelivered messages from the previous day — intentional, consistent with how intradayBars are discarded.
  </action>
  <verify>Run npm run build — TypeScript should compile with no errors about netWorthTriggerFiredToday being an unknown property.</verify>
  <done>netWorthTriggerFiredToday: boolean exists in GameState interface, initializes to false, is excluded from partialize, and resets to false in advanceDay set() call. pendingMessages: [] also in that same reset block.</done>
</task>

<task type="auto">
  <name>Task 2: Convert advanceDay messages to ScheduledMessages and add swing trigger in tickMarket</name>
  <files>src/store/useGameStore.ts</files>
  <action>
Make two sets of changes in src/store/useGameStore.ts:

PART A — Convert advanceDay wife and extra-contact messages to ScheduledMessages:

The Guru message and Loan Shark message should remain as direct thread pushes (their day-transition timing is correct). Only wife and extra-contact messages get scheduled.

Create a local array at the start of the message-building section in advanceDay:
    const newScheduledMessages: ScheduledMessage[] = [];

For the Wife message section (around lines 1298-1309):
- Instead of pushing wifeMessage directly into newThreads['Wife'].messages, create a ScheduledMessage:
    const wifeDeliverAt = 570 + Math.floor(Math.random() * 390);
    newScheduledMessages.push({
      message: wifeMessage,
      deliverAt: wifeDeliverAt,
      delivered: false,
    });
- Remove the direct push to newThreads['Wife'].messages (the thread itself stays, just don't push the message body now).
- The Wife thread already exists from getInitialState(), so thread delivery in tickMarket will work fine.

For the extra contacts section (around lines 1343-1368, the selected.forEach block):
- Currently each contact's message is pushed to newThreads[contact.name].messages.
- Instead, collect as ScheduledMessage:
    const contactDeliverAt = 570 + Math.floor(Math.random() * 390);
    newScheduledMessages.push({
      message: { id: `contact-${contact.name}-${nextDayNum}`, sender: contact.name, text, day: nextDayNum },
      deliverAt: contactDeliverAt,
      delivered: false,
    });
- IMPORTANT: Still create the thread entry in newThreads if it does not exist (for tickMarket delivery to work), but do not push the message body. Keep the existing thread-creation logic (the if (!newThreads[contact.name]) { ... } block) but remove the message push from it.

In the final set() call, use the collected array instead of the Task 1 reset:
    pendingMessages: newScheduledMessages,   // replaces the [] from Task 1 reset

PART B — Net-worth swing trigger in tickMarket:

In tickMarket (around lines 850-898), after the pendingMessages delivery block (around line 854) and after currentNetWorth is computed (line 868), add:

    let netWorthTriggerFired = state.netWorthTriggerFiredToday;
    let swingScheduled = [...newPendingMessages];

    if (!state.netWorthTriggerFiredToday && newIsOpen) {
      const dayOpenNW = state.netWorthBars.length > 0 ? state.netWorthBars[0].open : 0;
      if (dayOpenNW > 0) {
        const swingPct = Math.abs(currentNetWorth - dayOpenNW) / dayOpenNW;
        if (swingPct >= 0.10) {
          netWorthTriggerFired = true;
          const swingTier: PerformanceTier = currentNetWorth > dayOpenNW ? 'POSITIVE' : 'NEGATIVE';
          const swingText = getRandomTemplate('WIFE', swingTier, { netWorth: String(Math.round(currentNetWorth)) });
          swingScheduled = [
            ...swingScheduled,
            {
              message: {
                id: `wife-swing-${newTime}`,
                sender: 'Wife',
                text: swingText,
                day: state.day,
              },
              deliverAt: newTime,
              delivered: false,
            } as ScheduledMessage,
          ];
        }
      }
    }

In the set() call at the bottom of tickMarket, update:
- pendingMessages: newPendingMessages  becomes  pendingMessages: swingScheduled
- Add: netWorthTriggerFiredToday: netWorthTriggerFired

The ScheduledMessage type is imported from types.ts which is already imported. The Wife thread exists from getInitialState(), so delivery will work.
  </action>
  <verify>Run npm run build — zero TypeScript errors. Start game, advance day, watch the Wife thread — message should appear mid-session not at day start. Verify that a large portfolio move (achievable with debug +1h then a market event) triggers an additional wife message.</verify>
  <done>advanceDay pushes wife and extra-contact messages to newScheduledMessages with random deliverAt 570-959. tickMarket swing detection block present, gated by !netWorthTriggerFiredToday. TypeScript build clean.</done>
</task>

</tasks>

<verification>
- npm run build exits 0
- netWorthTriggerFiredToday in GameState type, initialized false, excluded from partialize, reset in advanceDay
- advanceDay does NOT push wife message directly to newThreads['Wife'].messages
- advanceDay set() uses pendingMessages: newScheduledMessages (not [])
- tickMarket has swingPct calculation block referencing netWorthTriggerFiredToday
- tickMarket set() uses swingScheduled and sets netWorthTriggerFiredToday
</verification>

<success_criteria>
1. Advancing a day no longer instantly shows all message threads — wife message appears at a random market-hours time.
2. A single wife message fires mid-session on a 10%+ net-worth move from day open, not repeatedly.
3. Previous day undelivered messages are discarded when advancing.
4. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/16-qol-polish/16-02-SUMMARY.md`
</output>
