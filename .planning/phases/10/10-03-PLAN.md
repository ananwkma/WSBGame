---
phase: 10-social-guru
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified: [src/store/useGameStore.ts, src/components/Phone/ChatApp.tsx, src/components/Laptop/GuruTube.tsx]
autonomous: true
requirements: [NARR-06]
must_haves:
  truths:
    - "Player receives multiple distinct messages every turn"
    - "New random contacts appear in the phone's message list"
    - "Contact list handles 5+ threads with scrolling and unread badges"
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Enhanced multi-message generation logic"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "src/components/Phone/ChatApp.tsx"
      via: "thread list state"
---

<objective>
Increase social engagement through higher message volume and variety, including the introduction of "Random Contact" messages.

Purpose: Make the "Ape" world feel alive and unpredictable.
Output: Narrative engine that generates 2-4 messages per turn and supports spontaneous character interactions.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10/10-RESEARCH.md
@src/store/useGameStore.ts
@src/components/Phone/ChatApp.tsx
</context>

<tasks>

<task type="auto">
  <name>Increase Daily Message Volume and Variety</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Modify the `nextTurn` logic in `useGameStore.ts` to generate 2-4 messages every turn (increased from the current 1-2).
    - Implement logic to ensure diversity in message senders (don't send 4 messages from "Ape Friend" in one day).
    - Randomize which characters speak on any given turn while prioritizing important plot-relevant senders like "Wife" and "Guru".
  </action>
  <verify>
    - Advance turns and count the number of new messages in the Phone app.
  </verify>
  <done>Message volume is significantly increased.</done>
</task>

<task type="auto">
  <name>Implement Random Contact Messaging</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Add a `generateRandomContactMessage` helper to the store.
    - Characters include: "IRS Auditor", "Margin Clerk", "Unknown Stalker", "Ex-Coworker", "Lamborghini Dealership".
    - These contacts should be added to the `threads` object when they first message.
    - Set unique avatars and initial "lastReadDay" so they show up as new.
  </action>
  <verify>
    - Verify that a new contact name (not from the initial list) appears in the thread list after a few turns.
  </verify>
  <done>Spontaneous interactions from random contacts are integrated into the narrative engine.</done>
</task>

<task type="auto">
  <name>Final Narrative UI Polish</name>
  <files>src/components/Laptop/GuruTube.tsx, src/components/Phone/ChatApp.tsx</files>
  <action>
    - Add visual indicators for "Unread" threads in the contact list (red dot or numeric badge).
    - Add a "Typing..." indicator animation for incoming messages in ChatApp if time allows (simple CSS dots).
    - Ensure GuruTube's chat sidebar scrolls correctly even with high message frequency.
  </action>
  <verify>
    - Test the messaging interface with 5+ active threads.
    - Ensure scrolling and selection work as expected.
  </verify>
  <done>The narrative and social UI is polished and robust.</done>
</task>

</tasks>

<verification>
Count messages per turn. Confirm at least 2 distinct characters message each turn. Verify random contacts appear and have correct avatars.
</verification>

<success_criteria>
- 2-4 messages generated per day.
- At least 3 "Random" contacts appear by day 10.
- Phone app handle 8+ threads without layout issues.
</success_criteria>

<output>
After completion, create `.planning/phases/10/10-03-SUMMARY.md`
</output>
