---
phase: 09-accurate-options-engine
plan: 09-02-PLAN
type: execute
wave: 2
depends_on: [09-01-PLAN]
files_modified: [src/store/useGameStore.ts]
autonomous: true
requirements: [TRADE-10, TRADE-12]

must_haves:
  truths:
    - "Stocks have unique initial IV levels: $GAME 150%, $POPC 80%, $APE 300%."
    - "IV increases (spikes) 1-2 turns before a narrative price SHIFT event."
    - "IV returns to base levels (IV Crush) immediately after a SHIFT occurs."
  artifacts:
    - path: "src/store/useGameStore.ts"
      provides: "Initialized IV state and turn-based IV dynamics"
  key_links:
    - from: "nextTurn logic"
      to: "eventQueue"
      via: "filter for future SHIFT events"
---

<objective>
Implement dynamic Implied Volatility (IV) logic, including base values, spikes before major events, and "IV Crush" after events occur.

Purpose: Simulate market anticipation and the subsequent collapse in option premiums after volatility events.
Output: Volatility management logic integrated into the game loop.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09/09-RESEARCH.md
@src/store/useGameStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Base IV for Stocks</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Update `getInitialState` to set the initial `iv` for each stock in the `stocks` record.
    - $GAME: 1.5 (150%)
    - $POPC: 0.8 (80%)
    - $APE: 3.0 (300%)
  </action>
  <verify>Check state in React dev tools or log initial state: stocks['$GAME'].iv should be 1.5.</verify>
  <done>Base IV is correctly initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IV Spike and Crush Logic</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    - Inside `nextTurn`, before processing the turn increment:
      - Iterate through stocks.
      - Check for `SHIFT` events in `nextDayNum + 1` or `nextDayNum + 2` for each stock.
      - If a future SHIFT is detected, increase stock IV by 1.0 (spike).
      - If a SHIFT event is occurring *this* turn (`nextDayNum`), reset that stock's IV to its base value (crush).
      - Otherwise, decay IV slowly towards base value (e.g., `iv = Math.max(baseIv, iv * 0.9)`).
  </action>
  <verify>Advance game turns and monitor stock IV. Verify it rises before SHIFTs and drops after.</verify>
  <done>Volatility dynamics are functional in the game loop.</done>
</task>

</tasks>

<verification>
- Start game, check options premiums for $GAME.
- Identify an upcoming $GAME SHIFT event (e.g., Day 4).
- Advance to Day 2 or 3, observe IV rise and premiums explode.
- Advance to Day 4 (event day), observe IV drop and premiums collapse (IV Crush).
</verification>

<success_criteria>
- IV reflects market anticipation of events.
- Options trading becomes significantly more risky/rewarding around events due to IV shifts.
</success_criteria>

<output>
After completion, create `.planning/phases/09/09-02-SUMMARY.md`
</output>
