---
phase: 10-social-guru
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Laptop/GuruTube.tsx, src/components/Laptop/MiniChart.tsx, src/components/Laptop/StockMarquee.tsx, src/components/Laptop/LaptopBrowser.css]
autonomous: true
requirements: [VIS-09]
must_haves:
  truths:
    - "GuruTube shows scrolling marquees with real-time stock prices"
    - "Mini SVG charts appear next to stock tickers in GuruTube"
    - "Charts turn green for gainers and red for losers"
  artifacts:
    - path: "src/components/Laptop/MiniChart.tsx"
      provides: "Lightweight SVG stock performance visualization"
      min_lines: 30
    - path: "src/components/Laptop/StockMarquee.tsx"
      provides: "CSS-animated scrolling stock ticker"
      min_lines: 40
  key_links:
    - from: "src/components/Laptop/GuruTube.tsx"
      to: "src/components/Laptop/MiniChart.tsx"
      via: "React component rendering"
    - from: "src/components/Laptop/GuruTube.tsx"
      to: "src/components/Laptop/StockMarquee.tsx"
      via: "React component rendering"
---

<objective>
Overhaul the GuruTube UI to feel like a high-energy financial news parody by adding dynamic SVG charts and CSS-driven scrolling marquees.

Purpose: Increase world immersion and provide more "glanceable" market data to the player.
Output: Dynamic GuruTube interface with live-updating visual elements.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10/10-RESEARCH.md
@src/components/Laptop/GuruTube.tsx
</context>

<tasks>

<task type="auto">
  <name>Create MiniChart and StockMarquee components</name>
  <files>src/components/Laptop/MiniChart.tsx, src/components/Laptop/StockMarquee.tsx</files>
  <action>
    - Create `MiniChart.tsx`: A small SVG component (approx 60x20px) that takes an array of price points and renders a polyline. It should color the line green (#00ff00 or palette equivalent) if the last price is >= first price, and red otherwise.
    - Create `StockMarquee.tsx`: A component that takes an array of stock objects (ticker, price, change) and renders them in a continuous horizontal loop. Use CSS keyframe animations for performance (do not use JS timers for the movement).
  </action>
  <verify>
    - Check that MiniChart renders a path element.
    - Check that StockMarquee has a CSS animation attached to its content.
  </verify>
  <done>Components exist and render correctly in isolation.</done>
</task>

<task type="auto">
  <name>Implement Marquee CSS and Refactor GuruTube Layout</name>
  <files>src/components/Laptop/LaptopBrowser.css, src/components/Laptop/GuruTube.tsx</files>
  <action>
    - Add `@keyframes scroll-left` to `LaptopBrowser.css` for the marquee effect.
    - Update `GuruTube.tsx` layout:
      - Add a "Top Ticker" (StockMarquee) below the header.
      - Add a "Trending Stocks" sidebar or footer section using `MiniChart` for each stock.
      - Ensure the layout is responsive to the laptop screen size and respects the 4-color palette.
      - Use `image-rendering: pixelated` on SVG elements to maintain the aesthetic.
  </action>
  <verify>
    - Open GuruTube in the game.
    - Verify marquees are moving smoothly.
    - Verify mini-charts are visible and correctly colored.
  </verify>
  <done>GuruTube UI is overhaul with animated elements and dynamic charts.</done>
</task>

</tasks>

<verification>
Check GuruTube app in the game. Ensure scrolling is infinite and smooth. Verify charts update when the day changes.
</verification>

<success_criteria>
- Multiple scrolling tickers visible in GuruTube.
- SVG Mini-charts correctly reflect the performance of $GAME, $POPC, and $APE.
- No jitter in animations during day transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/10/10-01-SUMMARY.md`
</output>
