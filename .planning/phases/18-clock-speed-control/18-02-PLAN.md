---
phase: 18-clock-speed-control
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified: []
autonomous: false
requirements:
  - UX-04

must_haves:
  truths:
    - "Speed button appears next to the market clock and is always visible"
    - "Clicking cycles through 1x → 2x → 5x → 1x with correct label"
    - "Market time visibly advances faster at higher speeds"
    - "Speed resets to 1x when NEXT DAY is pressed"
    - "Debug buttons (+5m, +1h) are absent from the UI"
  artifacts:
    - path: "src/hooks/useMarketClock.ts"
      provides: "Parameterized hook at correct interval"
      contains: "BASE_INTERVAL_MS / speed"
    - path: "src/App.tsx"
      provides: "Speed button, no debug buttons, NEXT DAY resets speed"
      contains: "clockSpeed"
  key_links:
    - from: "speed button click"
      to: "market clock interval"
      via: "cycleSpeed → setClockSpeed → useMarketClock re-runs effect"
      pattern: "useMarketClock\\(clockSpeed\\)"
---

<objective>
Human verification of the completed clock speed control feature. Confirms the speed toggle works correctly in-browser, market accelerates as expected, NEXT DAY resets speed, and debug buttons are gone.

Purpose: Catch any visual or behavioral issues (layout breakage, speed not changing, reset not firing) before closing the phase.
Output: Approved or issues reported for fix.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify clock speed control in-browser</name>
  <files>src/hooks/useMarketClock.ts, src/App.tsx</files>
  <action>Human verification of completed clock speed feature. Run the app and test all 8 checks listed in how-to-verify. No code changes in this task — observation only.</action>
  <what-built>
    - useMarketClock now accepts a speed param (1 | 2 | 5) and scales the tick interval to 2000ms / speed
    - App.tsx has local clockSpeed state cycling 1x → 2x → 5x → 1x on each click
    - A pixel-art speed button ("1x" / "2x" / "5x") sits inside the clock container below the MARKET status line
    - NEXT DAY onClick now calls advanceDay() + setClockSpeed(1) to reset speed each day
    - DEBUG/IS_DEBUG_MODE/skipTime removed; +5m and +1h buttons gone
    - NEXT DAY gate simplified to `disabled={marketTime < 960}` (IS_DEBUG_MODE no longer bypasses it)
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the game in a browser (localhost:5173 or whatever Vite reports).

    1. **Button presence:** Confirm the speed button appears below the MARKET OPEN/CLOSED status line inside the clock box. It should show "1x" on load.

    2. **Cycle through speeds:** Click the speed button three times. Labels should read: 1x → 2x → 5x → 1x. Confirm each click updates the label immediately.

    3. **Speed effect at 2x:** While at 2x, watch the clock time advance. It should advance roughly 2 minutes of in-game time per real second (compared to 1 minute per real second at 1x). The difference should be visibly noticeable within ~5 seconds.

    4. **Speed effect at 5x:** Switch to 5x. Clock should advance roughly 5x faster than 1x — very rapid progression visible immediately.

    5. **NEXT DAY reset:** Set speed to 5x. Wait until market closes (or it is already closed). Press NEXT DAY. Confirm the speed button resets to "1x" on the new day.

    6. **Debug buttons absent:** Confirm there are no +5m or +1h buttons anywhere in the UI, in any environment.

    7. **Events/sounds at speed:** At 2x or 5x, wait for a market event (price flash, news ticker update, or sound cue). Confirm events fire normally — not suppressed.

    8. **tsc clean:** Run `npx tsc --noEmit` in the project root. Confirm it exits with no errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe any issues found.</resume-signal>
  <verify>Run npm run dev; confirm all 8 in-browser checks in how-to-verify pass; run npx tsc --noEmit and confirm exit code 0.</verify>
  <done>All 8 checks pass: button visible, cycles correctly, clock accelerates at 2x/5x, resets on NEXT DAY, debug buttons absent, tsc clean.</done>
</task>

</tasks>

<verification>
All 8 human-verify checks in Task 1 must pass.
</verification>

<success_criteria>
Phase 18 complete when:
1. Speed button visible and cycling correctly in browser
2. Market clock visibly accelerates at 2x and 5x
3. Speed resets to 1x on NEXT DAY
4. No debug buttons in UI
5. tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/18-clock-speed-control/18-02-SUMMARY.md`
</output>
