---
phase: 12-multiple-endings-expansion
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/components/Feedback/EndingScreen.tsx
  - src/styles/pixel.css
autonomous: true
requirements:
  - NARR-10

must_haves:
  truths:
    - "ENDING_CONTENT is a Record<EndingType, ...> with all 10 keys — TypeScript enforces exhaustiveness"
    - "Each ending has a unique title, WSB-ironic description, a unique color string, and a 10-15 line ASCII art string"
    - "ASCII art renders in a <pre className='ending-ascii'> block below the description paragraph"
    - ".ending-ascii CSS class exists in pixel.css with monospace font, white-space: pre, overflow-x: auto, max 40-char art width"
    - "The EndingScreen component renders the ascii field from content — not hardcoded"
    - "Old ENDING_CONTENT keys MOON and LEGEND are gone"
  artifacts:
    - path: "src/components/Feedback/EndingScreen.tsx"
      provides: "10-entry ENDING_CONTENT map with title/description/color/ascii per ending"
      contains: "BREAK_EVEN"
    - path: "src/styles/pixel.css"
      provides: ".ending-ascii CSS class"
      contains: "ending-ascii"
  key_links:
    - from: "src/components/Feedback/EndingScreen.tsx ENDING_CONTENT"
      to: "EndingType (from types.ts)"
      via: "Record<EndingType, EndingContent> type annotation"
      pattern: "Record<EndingType"
    - from: "EndingScreen render"
      to: "content.ascii"
      via: "<pre className='ending-ascii'> conditional block"
      pattern: "ending-ascii"
---

<objective>
Rebuild EndingScreen.tsx with 10 WSB-ironic endings including unique colors and ASCII art. Add the .ending-ascii CSS class to pixel.css.

Purpose: The visible payoff of the phase — players see distinct, flavored endings instead of the 3 generic ones. TypeScript's Record<EndingType> annotation ensures no ending is accidentally omitted.
Output: Updated EndingScreen.tsx with full ENDING_CONTENT map + <pre> renderer; new .ending-ascii CSS class in pixel.css.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multiple-endings-expansion/12-CONTEXT.md
@.planning/phases/12-multiple-endings-expansion/12-RESEARCH.md
@.planning/phases/12-multiple-endings-expansion/12-01-SUMMARY.md
@src/components/Feedback/EndingScreen.tsx
@src/styles/pixel.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .ending-ascii CSS class to pixel.css</name>
  <files>src/styles/pixel.css</files>
  <action>
After the `.ending-description` block (currently ends around line 67), add the new class:

```css
.ending-ascii {
  font-family: monospace;
  font-size: 0.65rem;
  line-height: 1.3;
  white-space: pre;
  text-align: left;
  display: inline-block;
  margin: 0 auto 2rem auto;
  color: inherit;
  overflow-x: auto;
  max-width: 100%;
}
```

The `.ending-card` already has `text-align: center` which centers the `display: inline-block` pre element. Keep art at 40 chars wide max in content — `overflow-x: auto` is a safety valve only.
  </action>
  <verify>Open src/styles/pixel.css and confirm `.ending-ascii` block exists with `white-space: pre` and `overflow-x: auto`.</verify>
  <done>.ending-ascii class is present in pixel.css with all required properties.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ENDING_CONTENT and update EndingScreen render</name>
  <files>src/components/Feedback/EndingScreen.tsx</files>
  <action>
Replace the entire file with the updated version. Keep the component structure identical — only change ENDING_CONTENT (full rebuild) and add the `<pre>` block to the render.

**Type annotation:** Add `interface EndingContent` and type ENDING_CONTENT as `Record<EndingType, EndingContent>` for TypeScript exhaustiveness.

**ENDING_CONTENT — all 10 entries with WSB-ironic copy, unique colors, and 10-15 line ASCII art:**

Tone rules (from CONTEXT.md): WSB-ironic throughout, dark humor, self-aware. Even the rich endings are unhinged. Copy feels like r/wallstreetbets post titles.

Colors: Choose distinct values. The PaletteFilter will remap them — treat color as flavor. Use values that differ in brightness to survive the 4-color remap (dark/mid/light spectrum). Per research, existing endings used #ff0000, #ffffff, #00ff00.

```typescript
import React from 'react';
import { useGameStore } from '../../store/useGameStore';
import type { EndingType } from '../../store/types';

interface EndingContent {
  title: string;
  description: string;
  color: string;
  ascii: string;
}

const ENDING_CONTENT: Record<EndingType, EndingContent> = {
  MENDYS: {
    title: "WENDY'S IS HIRING",
    description: "The market has spoken. You have been found financially unworthy. The good news: Dave's Double combo is still $8.99 and your new coworkers won't ask about your portfolio.",
    color: '#ff0000',
    ascii: `
   ___________
  |           |
  |  WENDY'S  |
  |___________|
  |  SPATULA  |
  |   RANK:   |
  |  ACHIEVED |
  |___________|
       |
   ____|____
  |  APRON  |
  |_________|
    `,
  },

  BREAK_EVEN: {
    title: 'BREAK EVEN BABY',
    description: "After 10 days of pure psychological warfare, you ended up roughly where you started. A savings account would have done this. With less crying.",
    color: '#888888',
    ascii: `
  ___     ___
 /   \\   /   \\
| +$$ | | -$$ |
 \\___/   \\___/
    |       |
    +---+---+
        |
     = $0 PL
    (you tried)
    `,
  },

  SMALL_WINS: {
    title: 'SMALL WINS',
    description: "You made some money. Not 'quit your job' money. Not 'tell people at parties' money. But money. Your therapist calls this 'healthy boundaries with risk.' You call it disappointing.",
    color: '#44aa44',
    ascii: `
      $$$
     $$$$$
    $$$$$$$
   ----+----
   GAINS: ✓
   YACHT: ✗
   DIGNITY: ✗
   ----------
   net: fine
    `,
  },

  TENDIES: {
    title: 'TENDIES',
    description: "You made real money. The kind that makes the apes on the forum seethe. Your wife's boyfriend asked if he could borrow some. You said no. Today was a good day.",
    color: '#ffaa00',
    ascii: `
    🍗🍗🍗🍗🍗
   /===========\\
  |  TENDIES   |
  |   GAINED   |
   \\===========/
    |   |||   |
    | GAINZ!! |
    |_________|
    `,
  },

  TO_THE_MOON: {
    title: 'TO THE MOON',
    description: "Seven figures. You did it. You are a financial genius, a market wizard, a god among degenerates. Your wife is calling you. It's from a different area code. You don't care.",
    color: '#00ff00',
    ascii: `
        *  .  *
      .   ___  .
     *   /   \\   *
        | 1M+ |
         \\___/
           |
       ====|====
       ROCKET GO
       BRRRRRRR
       =========
    `,
  },

  HEDGE_FUND_DARLING: {
    title: 'HEDGE FUND DARLING',
    description: "Eight figures. Wall Street wants to hire you. CNBC wants to interview you. You want to post loss porn 'for old times sake.' Some things money can't fix.",
    color: '#0088ff',
    ascii: `
   ___________
  | PORTFOLIO |
  |  MANAGER  |
  |___________|
  |  AUM: 10M+|
  |  FEES: 2/20|
  |  ALPHA: yes|
  |___________|
  [still shitposting]
    `,
  },

  WOLF_OF_WALL_STREET: {
    title: 'WOLF OF WALL STREET',
    description: "Nine figures. You are a menace. The SEC has a file on you. Your broker sends you a holiday card. You own three Roombas you've never seen. Life is noise.",
    color: '#ff00ff',
    ascii: `
   /\\  WOLF  /\\
  /  \\  OF  /  \\
 / $$ \\ WSB/ $$ \\
/______\\  /______\\
|  SEC  ||  GAINS |
| WATCH ||  100M+ |
|_______||________|
 \\ YOLO /\\ PRINT /
  \\____/  \\____/
    `,
  },

  PRIVATE_ISLAND: {
    title: 'PRIVATE ISLAND',
    description: "You bought a private island. Your wife's boyfriend lives there too. You don't care. You have a second island. You are beyond human concerns now. The forum calls you a legend. You haven't logged on in three years.",
    color: '#ffffff',
    ascii: `
   ~  🌴  ~  ✈  ~
  ~~~~~~~~~~~~~~~
  ~  ISLAND OF  ~
  ~  TENDERNESS ~
  ~~~~~~~~~~~~~~~
  ~ wife's BF:  ~
  ~    also     ~
  ~    here     ~
  ~~~~~~~~~~~~~~~
  (u don't care)
    `,
  },

  DEBT_SPIRAL: {
    title: 'DEBT SPIRAL',
    description: "The sharks came calling. Turns out borrowing money to buy options on meme stocks has some downside. Who knew. The loan is now larger than everything you own. Mathematically, you have negative money.",
    color: '#ff4400',
    ascii: `
   $$$  DOWN  $$$
    $$  WE    $$
     $  GO    $
      \\      /
       \\    /
        \\  /  <-- you
         \\/
   ________________
   |  LOAN SHARK  |
   |  is smiling  |
   |______________|
    `,
  },

  PAPER_HANDS: {
    title: 'PAPER HANDS',
    description: "You sold. You actually sold. That position you dumped for a 3% gain? It would have been worth over a million dollars today. The apes know. The forum will never forget. You will wake up at 3am about this for years.",
    color: '#ffff00',
    ascii: `
   ___     ___
  /   \\   /   \\
 | YOU |   |$1M+|
 | SOLD|   |GONE|
  \\___/     \\___/
     \\       /
      \\     /
    PAPER HANDS
    HAVE SPOKEN
   YOU COULD'VE
    `,
  },
};
```

**Updated render block** — add the `<pre>` after the description paragraph:

```tsx
export const EndingScreen: React.FC<EndingScreenProps> = ({ result }) => {
  const resetGame = useGameStore((state) => state.resetGame);
  const finalNetWorth = useGameStore((state) => state.finalNetWorth) ?? 0;
  const karma = useGameStore((state) => state.karma);
  const content = ENDING_CONTENT[result];

  return (
    <div className="ending-screen-overlay">
      <div className="ending-card" style={{ border: `4px double ${content.color}` }}>
        <h1 className="ending-title" style={{ color: content.color }}>{content.title}</h1>
        <p className="ending-description">{content.description}</p>
        {content.ascii && (
          <pre className="ending-ascii">{content.ascii}</pre>
        )}
        <div className="ending-stats">
          <StatRow label="FINAL NET WORTH" value={`$${(finalNetWorth / 100).toLocaleString()}`} />
          <StatRow label="KARMA" value={karma.toLocaleString()} />
        </div>
        <button className="pixel-button restart-button" onClick={resetGame}>
          RESTART GAME
        </button>
      </div>
    </div>
  );
};

const StatRow = ({ label, value }: { label: string, value: string }) => (
  <div className="stat-row">
    <span>{label}</span>
    <span>{value}</span>
  </div>
);
```

**Important:** The ASCII art strings use template literals. Ensure no trailing spaces break the art alignment. Keep each art piece to 40 chars max width. The emoji characters (🍗, 🌴, ✈) may not render in the Game Boy palette filter context — use text/symbol alternatives if the game's rendering constraints make emoji problematic. Claude's discretion per CONTEXT.md.

Remove the stale import of `useGameStore` if karma is no longer displayed — but it IS still displayed (karma stat row remains), so keep both imports.
  </action>
  <verify>
1. `npx tsc --noEmit` — 0 errors (Record&lt;EndingType, EndingContent&gt; exhaustiveness check passes).
2. `grep -n "MOON\|LEGEND" src/components/Feedback/EndingScreen.tsx` — 0 matches (old keys gone).
3. `grep -c "title:" src/components/Feedback/EndingScreen.tsx` — returns 10 (one per ending).
4. `grep -n "ending-ascii" src/components/Feedback/EndingScreen.tsx` — shows the &lt;pre&gt; block.
  </verify>
  <done>EndingScreen.tsx has all 10 endings with unique titles, descriptions, colors, and ASCII art. TypeScript validates exhaustiveness. The &lt;pre className="ending-ascii"&gt; block renders below the description. Old MOON and LEGEND keys are absent.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with 0 errors across the entire project.
2. `grep -rn "'MOON'\|'LEGEND'" src/` returns 0 matches in .ts/.tsx files (these were EndingType values — plain string literals like "TO THE MOON" in dialogue are fine).
3. `grep -n "BREAK_EVEN\|SMALL_WINS\|TENDIES\|PAPER_HANDS\|DEBT_SPIRAL" src/components/Feedback/EndingScreen.tsx` shows all 10 keys present.
4. `grep -n "ending-ascii" src/styles/pixel.css` shows the CSS class.
5. `grep -n "ending-ascii" src/components/Feedback/EndingScreen.tsx` shows the &lt;pre&gt; render block.
</verification>

<success_criteria>
- ENDING_CONTENT is Record&lt;EndingType, EndingContent&gt; with all 10 entries — TypeScript enforces this
- Each entry has unique title, WSB-ironic description, unique color, and 10-15 line ASCII art
- ASCII art renders via &lt;pre className="ending-ascii"&gt; below description
- .ending-ascii class in pixel.css has white-space: pre, monospace, overflow-x: auto
- TypeScript compiles clean across entire project
</success_criteria>

<output>
After completion, create `.planning/phases/12-multiple-endings-expansion/12-02-SUMMARY.md`
</output>
