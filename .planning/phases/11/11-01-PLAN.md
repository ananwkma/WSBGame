---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/store/types.ts, src/data/messageTemplates.ts, src/store/useGameStore.ts]
autonomous: true
requirements: [NARR-08, MECH-01]

must_haves:
  truths:
    - "Wife message tier is determined by net worth ranges"
    - "GameState tracks sharkDebt separate from cash"
  artifacts:
    - path: "src/data/messageTemplates.ts"
      provides: "14-bracket configuration and selector"
    - path: "src/store/types.ts"
      provides: "WifeBracket type and updated GameState"
  key_links:
    - from: "src/store/useGameStore.ts"
      to: "src/data/messageTemplates.ts"
      via: "getWifeBracket helper"
---

<objective>
Implement the logic and types for the 14-tier granular narrative system and the foundation for debt mechanics.

Purpose: Move from binary performance sentiment to high-fidelity wealth-based narrative and prepare for debt features.
Output: Refactored message selection logic and expanded state model.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11/11-RESEARCH.md
@src/store/types.ts
@src/data/messageTemplates.ts
@src/store/useGameStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Types and Configuration</name>
  <files>src/store/types.ts, src/data/messageTemplates.ts</files>
  <action>
    1. In `src/store/types.ts`:
       - Add `WifeBracket` type (14 tiers: BANKRUPT, BROKE, STRUGGLING, WORRIED, CONCERNED, BASELINE, COMFORTABLE, WELL_OFF, RICH, MILLIONAIRE, MULTI_MILLIONAIRE, DECA_MILLIONAIRE, CENT_MILLIONAIRE, BILLIONAIRE).
       - Add `sharkDebt: number` to `GameState`.
       - Add `isMarginCall: boolean` to `GameState` (groundwork).
    2. In `src/data/messageTemplates.ts`:
       - Define `WIFE_BRACKETS` configuration array with min/max ranges in CENTS (e.g., BROKE: 0 to 1,000,000).
       - BASELINE should be centered around the starting $100k (7.5M to 12.5M cents).
       - Refer to 11-RESEARCH.md for the specific 14 tiers and their approximate ranges.
  </action>
  <verify>Check that `WifeBracket` type and `WIFE_BRACKETS` array are correctly defined.</verify>
  <done>Types and configuration are ready for implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Range-Based Selector</name>
  <files>src/data/messageTemplates.ts</files>
  <action>
    1. Create `getWifeBracket(netWorthInCents: number): WifeBracket` helper function.
    2. Refactor `MESSAGE_TEMPLATES` to separate `WIFE` messages into a new `WIFE_MESSAGES` object keyed by `WifeBracket`.
    3. Keep existing `MESSAGE_TEMPLATES` for other categories (GURU, APES, etc.) using `PerformanceTier`.
    4. Update `getRandomTemplate` to handle the `WIFE` category separately by using `getWifeBracket` or create a new `getRandomWifeTemplate` function.
  </action>
  <verify>Verify `getWifeBracket` returns 'BANKRUPT' for -100 and 'BILLIONAIRE' for 200,000,000,000.</verify>
  <done>Logic for selecting messages based on exact net worth is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Update Store Logic</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    1. Update `getInitialState` to initialize `sharkDebt: 0` and `isMarginCall: false`.
    2. In the `nextTurn` action:
       - Calculate `netWorth` at the end of the turn (using `getNetWorth()`).
       - Use the new `getWifeBracket` (or updated `getRandomTemplate`) to select the appropriate narrative message for the Wife.
       - Ensure `wifeTier` (now `wifeBracket`) is derived from the exact net worth instead of the old 1.2/0.8 ratio.
  </action>
  <verify>Run a turn and check if the Wife's message changes as net worth moves across brackets.</verify>
  <done>Game loop uses the granular narrative system.</done>
</task>

</tasks>

<verification>
- `getWifeBracket` correctly maps net worth to the 14 tiers.
- `sharkDebt` is present in the game state.
- Wife messages are triggered using the new range-based logic in `nextTurn`.
</verification>

<success_criteria>
- Narrative system supports 14 tiers for the Wife.
- Game state includes debt tracking foundation.
- No regression in existing 3-tier sentiment system for other characters.
</success_criteria>

<output>
After completion, create `.planning/phases/11/11-01-SUMMARY.md`
</output>
