---
phase: 17-phone-lock-chart-overhaul
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/types.ts
  - src/store/useGameStore.ts
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "previousDayBars field exists in GameState with type Record<string, CandleBar[]>"
    - "advanceDay() snapshots intradayBars into previousDayBars before resetting intradayBars to {}"
    - "previousDayBars persists across page reload (not in partialize exclusion list)"
    - "Persist version is 3 with migration that adds previousDayBars: {} to older saves"
  artifacts:
    - path: "src/store/types.ts"
      provides: "previousDayBars field in GameState interface"
      contains: "previousDayBars: Record<string, CandleBar[]>"
    - path: "src/store/useGameStore.ts"
      provides: "advanceDay snapshot + persist migration v3"
      contains: "previousDayBars: state.intradayBars"
  key_links:
    - from: "src/store/useGameStore.ts advanceDay()"
      to: "state.previousDayBars"
      via: "set() call before intradayBars reset"
      pattern: "previousDayBars: state\\.intradayBars"
    - from: "src/store/useGameStore.ts persist"
      to: "version: 3"
      via: "migrate branch"
      pattern: "version < 3"
---

<objective>
Add `previousDayBars` to the game store — a snapshot of the last completed trading day's 1-minute intraday bars. This enables the IntraChart to pre-populate with yesterday's data each morning.

Purpose: The chart overhaul in Plan 03 requires yesterday's bars to be available at day start. Without this store change, the chart starts empty every morning.
Output: `previousDayBars: Record<string, CandleBar[]>` in GameState + advanceDay snapshot logic + persist version 3 migration.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/types.ts
@src/store/useGameStore.ts
@.planning/phases/17-phone-lock-chart-overhaul/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add previousDayBars to GameState type and getInitialState</name>
  <files>src/store/types.ts, src/store/useGameStore.ts</files>
  <action>
In `src/store/types.ts`, add to the `GameState` interface (after `netWorthBars` on line 182):
```
previousDayBars: Record<string, CandleBar[]>;  // 1-min bars from the last completed trading day (keyed by ticker)
```

In `src/store/useGameStore.ts`, find `getInitialState()` and add `previousDayBars: {}` alongside the other empty-map initializations (near `intradayBars: {}`).

Do NOT add `previousDayBars` to the `partialize` exclusion list — it must survive page reload so the chart pre-populates correctly on refresh. The current partialize on line 1498 excludes `intradayBars, netWorthBars, pendingMessages, activeEvents, scheduledEvents, marketTime, marketIsOpen, bigGainTicker, bigLossTicker, netWorthTriggerFiredToday` — leave those unchanged and do not add `previousDayBars`.
  </action>
  <verify>Run `npx tsc --noEmit` — should exit 0 with no errors about `previousDayBars`.</verify>
  <done>`previousDayBars` exists in the `GameState` interface and in the initial state with value `{}`; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot intradayBars into previousDayBars in advanceDay + persist migration v3</name>
  <files>src/store/useGameStore.ts</files>
  <action>
In `advanceDay()`, inside the `set()` call (around line 1463), add the snapshot BEFORE the `intradayBars: {}` reset line:
```typescript
previousDayBars: state.intradayBars,  // capture current day's 1-min bars before reset
intradayBars: {},
```

The `state` reference here is the snapshot passed to `set()` — use `state.intradayBars` (not `get().intradayBars`) so it captures the value at the time of the set call.

Update the persist configuration:
1. Change `version: 2` to `version: 3` (line ~1481).
2. Add a new migration branch after the existing `version < 2` block:
```typescript
if (version < 3) {
  state = { ...state, previousDayBars: {} };
}
```
This ensures saves from Phase 16 load without crashing when the new field is accessed.

Do NOT change any other advanceDay logic — only add these two lines and the migration branch.
  </action>
  <verify>
1. Run `npx tsc --noEmit` — exits 0.
2. Grep for `previousDayBars: state.intradayBars` in useGameStore.ts — should appear.
3. Grep for `version: 3` in useGameStore.ts — should appear.
4. Grep for `version < 3` in migrate block — should appear.
  </verify>
  <done>advanceDay snapshots `intradayBars` into `previousDayBars` before reset; persist version is 3 with migration that seeds `previousDayBars: {}` for older saves; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from the project root — must exit 0. Check that `GameState` interface contains `previousDayBars: Record<string, CandleBar[]>`. Check that advanceDay contains `previousDayBars: state.intradayBars` before `intradayBars: {}`. Check that persist version is 3 with migration branch for `version < 3`.
</verification>

<success_criteria>
- `previousDayBars: Record<string, CandleBar[]>` present in `GameState` in types.ts
- `previousDayBars: {}` present in `getInitialState()`
- `advanceDay()` sets `previousDayBars: state.intradayBars` before `intradayBars: {}`
- `version: 3` in persist config
- Migration adds `previousDayBars: {}` for saves with `version < 3`
- `previousDayBars` NOT in the partialize exclusion list
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-phone-lock-chart-overhaul/17-01-SUMMARY.md`
</output>
