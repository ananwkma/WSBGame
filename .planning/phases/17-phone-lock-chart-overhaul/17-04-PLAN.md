---
phase: 17-phone-lock-chart-overhaul
plan: "04"
type: execute
wave: 3
depends_on:
  - "17-02"
  - "17-03"
files_modified: []
autonomous: false
requirements:
  - UX-03

must_haves:
  truths:
    - "TypeScript build is clean (tsc --noEmit exits 0)"
    - "Phone shows black lock screen when laptop is focused"
    - "Notification previews appear on lock screen when messages arrive while locked"
    - "Notification previews auto-dismiss after ~5 seconds"
    - "Chart TODAY tab has 1M, 10M, 30M timeframes"
    - "Chart ALL tab has 1H, 4H, 1D timeframes"
    - "Both LINE and CANDLE modes work on both chart tabs"
    - "Sliding window keeps bar sizes consistent as new bars arrive"
  artifacts:
    - path: "src/components/Apps/Phone/PhoneApp.tsx"
      provides: "Lock screen with timed notification previews"
      contains: "lock-screen"
    - path: "src/components/Trade/IntraChart.tsx"
      provides: "New timeframe system with sliding window"
      contains: "TODAY_TIMEFRAMES"
    - path: "src/store/useGameStore.ts"
      provides: "previousDayBars + persist v3"
      contains: "previousDayBars: state.intradayBars"
  key_links:
    - from: "src/App.tsx"
      to: "PhoneApp"
      via: "focus={focus} setFocus={setFocus} props"
      pattern: "PhoneApp.*focus"
    - from: "Robbinghood.tsx"
      to: "IntraChart"
      via: "previousDayBars prop"
      pattern: "previousDayBars=\\{"
---

<objective>
Verify the complete Phase 17 implementation: TypeScript clean build, followed by human visual checks of the lock screen behavior and chart overhaul.

Purpose: Both lock screen and chart are interactive/visual features that require human verification — a TypeScript check alone is insufficient.
Output: Confirmed working lock screen + chart overhaul; any bugs documented for gap closure.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript clean build</name>
  <files></files>
  <action>
Run `npx tsc --noEmit` from the project root. If there are TypeScript errors, fix them before proceeding to the checkpoint.

Common issues to watch for:
- `previousDayBars` missing from destructure in Robbinghood.tsx
- `FocusArea` import path wrong in PhoneApp.tsx (correct: `../../Shell/DualViewShell`)
- `AnimatePresence` not imported in PhoneApp.tsx from framer-motion
- `threads` subscription missing from PhoneApp.tsx (useGameStore import)
- `TODAY_TIMEFRAMES` / `ALL_TIMEFRAMES` referenced before declaration in IntraChart.tsx
- `LockNotif` interface declared inside the component body — move outside the component if TypeScript complains about declaration order

Fix any errors found until `npx tsc --noEmit` exits 0.
  </action>
  <verify>Exit code of `npx tsc --noEmit` is 0.</verify>
  <done>TypeScript build exits 0 with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify lock screen and chart overhaul</name>
  <files></files>
  <action>
Run `npm run dev` and open the game in browser. Perform all 7 checks below to verify the complete Phase 17 feature set. Document any failures and fix before marking complete.
  </action>
  <verify>
Run `npm run dev` and open the game in browser. Perform these checks:

**Lock Screen (3 checks):**
1. Game loads with laptop in focus — phone panel should show a black screen with "uPhone" text at top and "TAP TO UNLOCK" at bottom. (NOT the chat interface.)
2. Click the black phone area — phone switches to full phone UI (uMessage/readit tabs visible). Click laptop area — phone returns to black lock screen.
3. While phone is locked (laptop focused), advance the market a few ticks until a message arrives (or trigger NEXT DAY to get scheduled messages). A notification preview card should appear on the black screen showing sender name and truncated message text. Wait ~5 seconds — the notification card should fade out and disappear automatically without any interaction. Only messages that arrived AFTER the phone was locked should appear (not old ones from before it was locked).

**Chart Overhaul (4 checks):**
4. Open Robbinghood, select any stock, open the chart tab. TODAY tab shows buttons: 1M | 10M | 30M. ALL tab shows: 1H | 4H | 1D. Both LINE and CANDLE toggle works on both tabs without jumping to a different timeframe.
5. Switch to ALL tab, 1H view, toggle to CANDLE mode. Shows candle bars (not just a line). Switch to 4H — fewer, wider bars appear.
6. Advance to Day 2 (NEXT DAY). Open stock chart on Day 2. TODAY tab 1M view shows pre-populated bars on the left from Day 1 (not a completely empty chart).
7. Watch the chart for a few seconds in 1M mode. As new bars arrive, older bars slide left — the rightmost bar is always the most recent, bars maintain consistent width.
  </verify>
  <done>All 7 checks pass: lock screen works, notifications appear for new messages then auto-fade after ~5 seconds, chart tabs have correct timeframes, sliding window works, Day 2 pre-population confirmed.</done>
</task>

</tasks>

<verification>
All 7 human-verify checks pass. TypeScript build is clean.
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- Lock screen displays when laptop is focused; tap-to-unlock works
- Message notifications appear on lock screen for messages arriving while locked
- Notification previews auto-dismiss with fade after ~5 seconds
- Chart TODAY tab: 1M/10M/30M for line and candle
- Chart ALL tab: 1H/4H/1D for line and candle
- Day 2 chart pre-populated with Day 1 bars
- Sliding window maintains consistent bar widths
</success_criteria>

<output>
After completion, create `.planning/phases/17-phone-lock-chart-overhaul/17-04-SUMMARY.md`
</output>
