---
phase: 08
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified: [src/components/Apps/Phone/ChatApp.tsx, src/components/Apps/Phone/MessageList.tsx, src/components/Apps/Phone/MessageThread.tsx, src/components/Apps/Phone/Phone.css]
autonomous: true
requirements: [NARR-02]
---

<objective>
Implement the new UI for the threaded messaging system, providing a contact list view with unread badges and a detailed thread view for each conversation.

Purpose: Improve the narrative flow and social UX.
Output: Master-Detail messaging interface with unread indicators and conversation history.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-02-SUMMARY.md
@src/components/Apps/Phone/ChatApp.tsx
@src/components/Apps/Phone/Phone.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageList and MessageThread Components</name>
  <files>src/components/Apps/Phone/MessageList.tsx, src/components/Apps/Phone/MessageThread.tsx, src/components/Apps/Phone/Phone.css</files>
  <action>
    Create two new functional components in `src/components/Apps/Phone/`.
    - `MessageList.tsx`:
      - Receive a list of sorted threads from `ChatApp.tsx`.
      - Render each thread as a row with an avatar (emoji), contact name, last message snippet, and a conditional "unread" dot (based on `lastReadDay`).
    - `MessageThread.tsx`:
      - Receive a specific thread object and a back button handler.
      - Render the conversation history (reversed, newest at bottom).
      - Include a "NEW MESSAGES" separator line if some messages in the history have a `day` greater than the thread's `lastReadDay` (captured at entry).
    - Update `Phone.css` to add styles for the contact list, avatars, and unread badges.
  </action>
  <verify>Check for the existence and basic implementation of MessageList.tsx and MessageThread.tsx.</verify>
  <done>Components for Master-Detail messaging view are created and styled.</done>
</task>

<task type="auto">
  <name>Task 2: Update ChatApp.tsx Controller</name>
  <files>src/components/Apps/Phone/ChatApp.tsx</files>
  <action>
    Overhaul the `ChatApp.tsx` component to act as a navigation controller.
    - Select `threads` and `setThreadRead` from the store.
    - Use local state `selectedContactId` to track the current view.
    - Logic:
      - If `selectedContactId` is null, render `MessageList`.
      - If `selectedContactId` is not null, render `MessageThread` for that contact.
    - When selecting a contact:
      - Store current `lastReadDay` locally for the "New Messages" separator in `MessageThread`.
      - Call `setThreadRead(contactId)` from the store immediately to mark messages as read.
  </action>
  <verify>Confirm ChatApp.tsx correctly switches between list and thread views based on state.</verify>
  <done>ChatApp successfully manages navigation between contacts and conversation detail.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auto-Sorting for Contacts</name>
  <files>src/components/Apps/Phone/ChatApp.tsx</files>
  <action>
    In `ChatApp.tsx`, derive a sorted array of threads from the `threads` record.
    - Sort logic: `Object.values(threads).sort((a, b) => b.messages[0].day - a.messages[0].day)` (or whatever the latest message day is).
    - Ensure that any thread with a new message automatically moves to the top of the `MessageList`.
  </action>
  <verify>Check ChatApp.tsx and ensure the list of threads is sorted by the most recent message day.</verify>
  <done>Contacts are automatically sorted with the most recent activity appearing at the top.</done>
</task>

</tasks>

<verification>
Open the Chat app on the phone.
Verify the contact list displays current threads.
Click on a contact and verify the thread view opens with the correct history.
Confirm unread dots appear when new messages arrive and disappear after viewing.
</verification>

<success_criteria>
- Messaging app has a list and detail view.
- Unread badges correctly reflect unviewed messages.
- "New Messages" separator appears when entering a thread with unread content.
- Contacts are sorted by most recent activity.
</success_criteria>

<output>
After completion, create `.planning/phases/08/08-03-SUMMARY.md`
</output>
