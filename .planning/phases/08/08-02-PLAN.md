---
phase: 08
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/store/types.ts, src/store/useGameStore.ts, src/components/Laptop/GuruTube.tsx]
autonomous: true
requirements: [NARR-02, NARR-04, NARR-05]
---

<objective>
Refactor messaging state to support threaded conversations, unread tracking, and implement a reactive "Wife" sentiment engine that responds to the player's financial performance.

Purpose: Provide a more realistic social experience and narrative feedback loop.
Output: Threaded message store and dynamic narrative generation.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08/08-RESEARCH.md
@src/store/types.ts
@src/store/useGameStore.ts
@src/components/Laptop/GuruTube.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor State to Threaded Messaging</name>
  <files>src/store/types.ts, src/store/useGameStore.ts</files>
  <action>
    Refactor the global state to support threaded conversations.
    - In `types.ts`, add `Thread` interface: `{ contactName: string, avatar: string, lastReadDay: number, messages: Message[] }`.
    - Update `GameState` in `types.ts`: Replace `messages: Message[]` with `threads: Record<string, Thread>`.
    - In `useGameStore.ts`:
      - Update `getInitialState`: Initialize `threads` with groups from `INITIAL_EVENTS`. Assign emojis as avatars (e.g., 👩 for Wife, 🦍 for Ape Friend).
      - Add `setThreadRead(sender: string)` action to update a thread's `lastReadDay` to the current game day.
    - Update `Message` interface to include `day: number`.
  </action>
  <verify>Check types.ts for Thread definition and useGameStore.ts for the new threads structure.</verify>
  <done>Store state successfully uses a record-based thread structure with unread tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Wife Sentiment Engine</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    Add dynamic message generation logic to the `nextTurn` action in `useGameStore.ts`.
    - Calculate current performance relative to starting capital ($100k).
    - Every turn, generate a "Wife" message:
      - **Positive** (> $120k): Use templates like "Honey, I'm so proud of you! I'm looking at houses in the Hamptons."
      - **Negative** (< $80k): Use templates like "I saw the bank account. Please tell me it's a mistake."
      - **Neutral** (otherwise): General check-in messages.
    - Unshift the new message into the "Wife" thread.
    - Ensure the message has a unique ID (`wife-day-${day}`) and the current `day`.
  </action>
  <verify>Check nextTurn in useGameStore.ts for the Wife sentiment logic and message generation.</verify>
  <done>Wife character sends dynamic messages reacting to the player's net worth changes.</done>
</task>

<task type="auto">
  <name>Task 3: Update Event Processing for Threads</name>
  <files>src/store/useGameStore.ts</files>
  <action>
    Modify `processEvents` in `useGameStore.ts` to handle the new threaded structure.
    - Instead of unshifting into a flat `messages` array, find or create the appropriate thread in the `threads` record based on the `sender` of the event payload.
    - Push the message into that thread's `messages` array.
    - Ensure that initial game events (Ape Friend, Brokerage, etc.) are correctly routed to their respective threads.
  </action>
  <verify>Check processEvents and confirm it correctly updates specific threads instead of a global list.</verify>
  <done>Incoming narrative events are correctly categorized into conversation threads.</done>
</task>

<task type="auto">
  <name>Task 4: Update GuruTube Selector</name>
  <files>src/components/Laptop/GuruTube.tsx</files>
  <action>
    Update `src/components/Laptop/GuruTube.tsx` to select the 'Crypto Guru' thread from `state.threads` instead of the removed `state.messages` array.
    - Ensure it still displays guru advice correctly from the new threaded structure.
  </action>
  <verify>Verify GuruTube displays current guru messages after the state refactor.</verify>
  <done>GuruTube component is correctly integrated with the new threaded state.</done>
</task>

</tasks>

<verification>
Verify that the `threads` record is correctly populated on game start.
Verify that `nextTurn` adds a message to the Wife thread.
Confirm that `setThreadRead` correctly updates the `lastReadDay`.
Verify GuruTube functionality is restored.
</verification>

<success_criteria>
- Messaging state uses a Thread-based record structure.
- Every turn, the Wife character sends a reactive message based on portfolio value.
- Narrative events are correctly routed to their respective threads.
- Laptop GuruTube remains functional.
</success_criteria>

<output>
After completion, create `.planning/phases/08/08-02-SUMMARY.md`
</output>
