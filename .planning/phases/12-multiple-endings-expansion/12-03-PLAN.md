---
phase: 12-multiple-endings-expansion
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified: []
autonomous: false
requirements:
  - NARR-10

must_haves:
  truths:
    - "All 10 endings render visually in the browser with distinct titles and colors"
    - "ASCII art is readable and does not overflow the card"
    - "No stale MOON or LEGEND EndingType string references exist anywhere in src/"
    - "TypeScript compiles with 0 errors"
  artifacts:
    - path: "src/components/Feedback/EndingScreen.tsx"
      provides: "Verified 10-ending screen"
      contains: "PAPER_HANDS"
    - path: "src/store/useGameStore.ts"
      provides: "Verified 10-ending detection cascade"
      contains: "DEBT_SPIRAL"
  key_links:
    - from: "nextTurn() ending detection"
      to: "EndingType values"
      via: "priority cascade — verified by human visual check of each ending"
      pattern: "WOLF_OF_WALL_STREET|PRIVATE_ISLAND"
---

<objective>
Verify the 10-ending system works end-to-end: clean TypeScript build, no stale references, and human visual confirmation that the ending screens render correctly.

Purpose: Catches rendering regressions, palette issues with ASCII art, and any stale EndingType string references outside the three known files.
Output: Confirmed working ending system, stale-reference audit complete.
</objective>

<execution_context>
@C:/Users/ananW/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multiple-endings-expansion/12-01-SUMMARY.md
@.planning/phases/12-multiple-endings-expansion/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale-reference audit and final build verification</name>
  <files></files>
  <action>
Run the following verification commands and fix any issues found:

**1. TypeScript clean build:**
```bash
npx tsc --noEmit
```
Expected: 0 errors. If errors exist, fix them before proceeding.

**2. Stale EndingType reference audit:**
```bash
grep -rn "'MOON'\|'LEGEND'" src/ --include="*.ts" --include="*.tsx"
```
Expected: 0 matches (these were EndingType string values — the string literal "TO THE MOON" in dialogue files is fine and will not match this pattern).

Also check for any remaining stale references to the old karma-based ending trigger:
```bash
grep -n "karma >= 50000" src/store/useGameStore.ts
```
Expected: 0 matches.

**3. All 10 endings present in both files:**
```bash
grep -c "BREAK_EVEN\|SMALL_WINS\|TENDIES\|TO_THE_MOON\|HEDGE_FUND_DARLING\|WOLF_OF_WALL_STREET\|PRIVATE_ISLAND\|DEBT_SPIRAL\|PAPER_HANDS\|MENDYS" src/components/Feedback/EndingScreen.tsx
grep -c "BREAK_EVEN\|SMALL_WINS\|TENDIES\|TO_THE_MOON\|HEDGE_FUND_DARLING\|WOLF_OF_WALL_STREET\|PRIVATE_ISLAND\|DEBT_SPIRAL\|PAPER_HANDS\|MENDYS" src/store/useGameStore.ts
```
Expected: at least 10 matches each.

**4. Start dev server:**
```bash
npm run dev
```
Leave running for human verification step.
  </action>
  <verify>
- `npx tsc --noEmit` exits with code 0
- `grep -rn "'MOON'\|'LEGEND'" src/` returns 0 matches in .ts/.tsx files
- `grep -n "karma >= 50000" src/store/useGameStore.ts` returns 0 matches
- Dev server starts without errors
  </verify>
  <done>TypeScript compiles clean, no stale references, dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of all 10 ending screens</name>
  <files></files>
  <action>
Pause for human to visually verify the 10 ending screens in the browser. Dev server is running from Task 1.

What was built:
- 10-ending state layer: types.ts (EndingType union, peakOpportunityCost) and useGameStore.ts (detection cascade, getOpportunityCost)
- 10-ending content layer: EndingScreen.tsx (ENDING_CONTENT with title/description/color/ascii per ending) and pixel.css (.ending-ascii class)
  </action>
  <verify>
Human approves by typing "approved" after confirming:
1. Visit http://localhost:5173, click "Next Turn" 10 times to reach ending screen
2. Title renders with unique color, description is readable, ASCII art appears below description in monospace
3. ASCII art does not overflow the card horizontally
4. MENDYS and BREAK_EVEN endings look visually distinct from each other
5. ASCII art is legible through the Game Boy palette filter
6. Restart button works
  </verify>
  <done>Human types "approved" confirming all 10 endings display correctly with readable ASCII art and no overflow.</done>
  <what-built>
10-ending system complete: state layer (types.ts + useGameStore.ts) and content layer (EndingScreen.tsx + pixel.css). The ending detection cascade covers all 10 endings with correct wealth thresholds and behavior trigger logic.
  </what-built>
  <how-to-verify>
1. Open the game in the browser (dev server running at http://localhost:5173 or similar).

2. The easiest approach: click "Next Turn" 10 times from a fresh game to reach the ending screen. Starting balance of $100k with no trades will land in BREAK_EVEN territory.

3. For each ending you want to spot-check, verify:
   - The title renders clearly with a unique color
   - The description text is readable and fits the card
   - The ASCII art appears BELOW the description in a monospace font
   - The ASCII art does not overflow the card horizontally
   - The card border color matches the title color

4. Specifically confirm the MENDYS and BREAK_EVEN endings look distinct (different colors despite both being "losing" outcomes).

5. Confirm the ASCII art is readable through the Game Boy palette filter — it does not need to be perfect color, but it should be legible.

6. Check that the restart button still works after reaching an ending.

Expected: All endings display correctly. ASCII art is legible. No JavaScript console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if endings look correct, or describe any visual issues (overflow, missing art, color problems) to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 12 complete when:
1. TypeScript compiles with 0 errors
2. No stale 'MOON' or 'LEGEND' EndingType references in src/
3. Human confirms endings render correctly in browser
4. All 10 ending keys present in both useGameStore.ts and EndingScreen.tsx
</verification>

<success_criteria>
- Clean tsc build
- Stale reference audit passes (0 matches for old EndingType values)
- Human visual approval of ending screen rendering
- All 10 endings are unique: distinct title, color, description, ASCII art
</success_criteria>

<output>
After completion, create `.planning/phases/12-multiple-endings-expansion/12-03-SUMMARY.md`
</output>
