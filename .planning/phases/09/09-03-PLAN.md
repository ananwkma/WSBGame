---
phase: 09-accurate-options-engine
plan: 09-03-PLAN
type: execute
wave: 3
depends_on: [09-01-PLAN, 09-02-PLAN]
files_modified: [src/components/Trade/OptionsChain.tsx, src/components/Trade/Robbinghood.tsx]
autonomous: true
requirements: [TRADE-10, TRADE-11]

must_haves:
  truths:
    - "Vega (V) is displayed for each option in the Options Chain."
    - "The current Implied Volatility (IV) % is visible in the stock detail view."
    - "Portfolio holdings for options display the current IV of the underlying."
  artifacts:
    - path: "src/components/Trade/OptionsChain.tsx"
      provides: "Updated Greeks display including Vega"
    - path: "src/components/Trade/Robbinghood.tsx"
      provides: "IV indicator in stock view and portfolio list"
---

<objective>
Update the UI to display the new volatility-related metrics (IV and Vega).

Purpose: Provide players with the necessary information to understand "IV Crush" and how volatility affects their option prices.
Output: Updated trading and portfolio interfaces.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Trade/OptionsChain.tsx
@src/components/Trade/Robbinghood.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vega to Options Chain</name>
  <files>src/components/Trade/OptionsChain.tsx</files>
  <action>
    - Update the Greeks display in `OptionsChain.tsx` to include Vega (V).
    - Format Vega to 2 or 3 decimal places.
    - Example display: Δ0.50 Γ0.001 Θ-12 V0.05
  </action>
  <verify>Open the options chain in the browser and verify that "V" is now displayed alongside Delta, Gamma, and Theta.</verify>
  <done>Vega is visible in the options chain.</done>
</task>

<task type="auto">
  <name>Task 2: Add IV Indicator to Stock View</name>
  <files>src/components/Trade/Robbinghood.tsx</files>
  <action>
    - Find the stock detail view section in `Robbinghood.tsx`.
    - Add a line displaying "Implied Volatility (IV): XX%".
    - Ensure it updates when the selected stock changes.
  </action>
  <verify>Select a stock in Robbinghood and verify the IV percentage is displayed and matches the expected base values ($GAME: 150%, etc.).</verify>
  <done>IV is visible in the stock detail view.</done>
</task>

<task type="auto">
  <name>Task 3: Update Portfolio View with IV</name>
  <files>src/components/Trade/Robbinghood.tsx</files>
  <action>
    - In the portfolio holdings list, specifically for options, add the current IV of the underlying stock.
    - This helps players see if their held options are being affected by IV Crush.
  </action>
  <verify>Buy an option and check the portfolio list. Verify IV is shown for the option position.</verify>
  <done>IV is visible in the portfolio view for options.</done>
</task>

</tasks>

<verification>
- Navigate through Robbinghood apps.
- Check $GAME, $POPC, and $APE to see different IV levels.
- Open options chains for each and verify Greek values (especially Vega) are present.
</verification>

<success_criteria>
- Players can clearly see IV and Vega metrics.
- The UI reflects the underlying mathematical changes made in Plan 01 and 02.
</success_criteria>

<output>
After completion, create `.planning/phases/09/09-03-SUMMARY.md`
</output>
